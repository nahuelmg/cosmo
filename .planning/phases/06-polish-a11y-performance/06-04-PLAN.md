---
phase: 06-polish-a11y-performance
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - .planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Lighthouse mobile (simulated 4G) LCP < 2.5s on /es, /es/personas, /es/publicaciones"
    - "Lighthouse CLS = 0 on all 8 Spanish pages"
    - "On /es/contacto, the Lighthouse 'Largest Contentful Paint element' diagnostic shows an element that is NOT the Maps iframe"
    - "`pnpm build` output reports 45 static routes (43 content + _not-found + _global-error), 0 dynamic — verified machine-readably via .next/prerender-manifest.json"
    - "`grep -r 'mailto\\|@.*\\.' .next/server/app --include='*.html'` (excluding twitter.com and schema.org lines) returns 0 matches — NAV-03 preserved"
    - "All Phase 6 changes are accounted for; the phase is ready for verifier sign-off"
  artifacts:
    - path: ".planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md"
      provides: "Final Phase 6 verification report: Lighthouse table, CLS table, static-output check, NAV-03 check, PERF-05 LCP element check"
      contains: "LCP < 2.5s, CLS = 0, Maps is NOT LCP, 45 static routes, 0 email"
  key_links:
    - from: ".next/prerender-manifest.json"
      to: "static-route count assertion"
      via: "node one-liner reading the manifest"
      pattern: "prerender-manifest"
    - from: ".next/server/app/**/*.html"
      to: "NAV-03 non-regression grep"
      via: "grep mailto/@ excluding twitter+schema"
      pattern: "mailto\\|@.*\\."
    - from: "Chrome DevTools Lighthouse (manual human runs)"
      to: "06-04-SUMMARY.md numeric table"
      via: "human-verify checkpoint; executor documents after human runs the audits"
---

<objective>
Final verification plan for Phase 6. Run Chrome DevTools Lighthouse (mobile, 4G throttled) on the three PERF-target pages, a CLS check on all 8 Spanish pages, verify the Maps iframe is NOT the LCP element on /es/contacto, and machine-check that all routes stayed static and NAV-03 held.

Purpose: Satisfies PERF-01 (static generation), PERF-02 (CLS = 0), PERF-04 (LCP < 2.5s on throttled 4G — Home, People, Publications), and PERF-05 (Maps does not contribute to Contact LCP). Also catches any regression in NAV-03 introduced by earlier Phase 6 edits.

**This plan has a human checkpoint.** Lighthouse is executed manually in the Chrome DevTools panel (per CONTEXT.md: "Performance: Chrome DevTools Lighthouse panel, manual runs per page. No Lighthouse CLI dependency"). Claude cannot drive the DevTools panel; the user runs it and reports numbers back to the executor, who then writes them into SUMMARY.md.

Output: 06-04-SUMMARY.md — the Phase 6 "done" artifact containing all six measurement categories (Lighthouse × 3 pages, CLS × 8, Maps-not-LCP × 1, static-route count, NAV-03 grep, overall pass/fail).
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-polish-a11y-performance/06-CONTEXT.md
@.planning/phases/06-polish-a11y-performance/06-RESEARCH.md
@.planning/phases/06-polish-a11y-performance/06-02-SUMMARY.md
@.planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Machine-readable static-output verification + NAV-03 non-regression grep</name>
  <files>.planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md (appended to at end of task 3)</files>
  <action>
Before asking the human to run Lighthouse, confirm the automatable checks pass. If they fail, Lighthouse is moot.

### Step 1 — Fresh build

```bash
pnpm build
```

Must exit 0. Capture the terminal table output — each content route should show `○ (Static)` not `ƒ (Dynamic)`. If any route shows `ƒ`, STOP and report — PERF-01 is violated and the phase cannot ship.

### Step 2 — Machine-readable prerender-manifest check

```bash
node -e "
const m = require('./.next/prerender-manifest.json');
const routes = Object.keys(m.routes);
const dyn = Object.keys(m.dynamicRoutes || {});
console.log('Static routes:', routes.length);
console.log('Dynamic route TEMPLATES:', dyn.length);
console.log('Dynamic routes (server-rendered):', 0);
if (routes.length < 43) { console.error('FAIL: fewer than 43 static routes'); process.exit(1); }
console.log('PASS');
"
```

Per RESEARCH.md Pattern 4, `dynamicRoutes` in the manifest is parametric *templates* (like `/[locale]/people/[slug]`) not server-rendered routes; their instantiations are in `routes`. Expected: ≥ 43 routes, 0 SSR.

Record the numbers for SUMMARY.md — staged in scratch; written in Task 3.

### Step 3 — NAV-03 grep

```bash
grep -r "mailto\|@.*\." .next/server/app --include="*.html" | grep -v "twitter" | grep -v "schema.org"
```

Expected: 0 lines. If any lines return, Phase 6 introduced an email regression — STOP, investigate which edit introduced it, and escalate. Do NOT proceed to Lighthouse until this is clean.

### Step 4 — Capture all numbers for Task 3

Keep the output of Steps 1-3 in a scratch file or in-memory. Task 3 writes them into 06-04-SUMMARY.md.
  </action>
  <verify>
1. `pnpm build` exits 0.
2. The node one-liner reports `routes.length ≥ 43` and exits 0.
3. The NAV-03 grep returns 0 lines.
  </verify>
  <done>
All automatable non-regression checks pass. Numbers captured for SUMMARY.md.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human-driven Lighthouse audits on mobile 4G preset (3 PERF-target pages) + CLS sweep (8 pages)</name>
  <what-built>
Claude has confirmed automatable checks pass (45 static routes, 0 mailto hits). The remaining measurements — Lighthouse LCP numbers and per-page CLS — require human operation of Chrome DevTools' Lighthouse panel, which no CLI can automate per CONTEXT.md's stated workflow. This checkpoint collects those numbers.
  </what-built>
  <how-to-verify>
### Prereq (in a separate terminal)

```bash
pnpm build && pnpm start
```
Wait for `Ready on http://localhost:3000`.

### Procedure — Lighthouse LCP × 3 pages (PERF-04)

For each of these three URLs:
1. `http://localhost:3000/es`
2. `http://localhost:3000/es/personas`
3. `http://localhost:3000/es/publicaciones`

Do:
- Open Chrome in a fresh, guest, or incognito window (disables extensions that skew measurements).
- Navigate to the URL.
- Open DevTools (F12) → Lighthouse tab.
- Settings: **Mode = Navigation**, **Device = Mobile**, **Categories ≥ Performance**, **Throttling = Simulated throttling (default 4G)**. Disable "Clear storage" if you want cached-run parity with production, or enable for cold-run measurement — use cold-run.
- Click "Analyze page load".
- When complete, record **LCP (seconds)**.
- **PASS bar:** LCP < 2.5s (PERF-04).

### Procedure — CLS × 8 pages (PERF-02/05)

For each of the 8 Spanish URLs (the 3 above + the 5 others — `/es/personas/esteban-calzetta`, `/es/investigacion`, `/es/contacto`, `/es/divulgacion`, `/es/journal-club`):

- Run Lighthouse (same mobile settings).
- Record **CLS** (a number between 0 and X).
- **PASS bar:** CLS = 0 (PERF-02 + the universal target). Anything > 0.001 fails — investigate the culprit (Lighthouse reports "Avoid large layout shifts" with specific elements).

### Procedure — Maps iframe is NOT LCP on /es/contacto (PERF-05)

On the `/es/contacto` Lighthouse report:
- Expand the "Largest Contentful Paint element" diagnostic.
- Confirm the element reported is NOT the Maps iframe. Expected: first `<h1>` or the address text block above the fold (per RESEARCH.md Pattern 5).
- If Maps IS the LCP: FAIL — the MapEmbed's IntersectionObserver facade is not working as intended. Investigate and escalate.

### Return numbers as a block

Report back in this format (copy-paste friendly):

```
LCP /es: {X.Xs}
LCP /es/personas: {X.Xs}
LCP /es/publicaciones: {X.Xs}

CLS /es: {0.XX}
CLS /es/personas: {0.XX}
CLS /es/personas/esteban-calzetta: {0.XX}
CLS /es/investigacion: {0.XX}
CLS /es/publicaciones: {0.XX}
CLS /es/contacto: {0.XX}
CLS /es/divulgacion: {0.XX}
CLS /es/journal-club: {0.XX}

Contact LCP element: {e.g. "h1.text-…" or "address>p"}
```
  </how-to-verify>
  <resume-signal>Paste the numbers block above, or describe issues (e.g. "LCP on /es/publicaciones is 3.1s — likely cause: …"). If any measurement fails the bar, we'll halt here and investigate before writing SUMMARY.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 06-04-SUMMARY.md with all verification numbers + overall phase pass/fail</name>
  <files>.planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md</files>
  <action>
Compile everything from Task 1 (machine checks) and Task 2 (human Lighthouse numbers) into the final Phase 6 verification artifact.

Layout:

```markdown
# Phase 6 · Plan 04 — Performance & Final Verification

**Completed:** {date}

## PERF-01 — Static output (all pages are SSG)

- `pnpm build` exit code: 0
- Prerender manifest check: `routes.length = {N}` (expected ≥ 43)
- Dynamic (SSR) routes in manifest: 0
- Verdict: **PASS** / FAIL

## PERF-02 / PERF-05 — CLS = 0 on every page + Maps is not Contact LCP

| Page | CLS | Pass? |
|------|----:|:-----:|
| /es | 0.00 | ✓ |
| /es/personas | 0.00 | ✓ |
| /es/personas/esteban-calzetta | 0.00 | ✓ |
| /es/investigacion | 0.00 | ✓ |
| /es/publicaciones | 0.00 | ✓ |
| /es/contacto | 0.00 | ✓ |
| /es/divulgacion | 0.00 | ✓ |
| /es/journal-club | 0.00 | ✓ |

### Contact LCP element (PERF-05)

- Reported LCP element: `{element}` (e.g. `h1` in address block)
- Maps iframe LCP? **No**
- Verdict: **PASS** / FAIL

## PERF-03 — Google Maps lazy-loaded (verification of carried invariant)

From codebase: `src/components/contact/MapEmbed.tsx` uses IntersectionObserver with `rootMargin: 200px` (RESEARCH.md Pattern 5). Confirmed by the PERF-05 LCP-element check above — Maps did not appear as LCP.

## PERF-04 — LCP < 2.5s mobile 4G on Home, People, Publications

| Page | LCP (s) | Pass? |
|------|--------:|:-----:|
| /es | {X.X} | ✓ |
| /es/personas | {X.X} | ✓ |
| /es/publicaciones | {X.X} | ✓ |

Throttling: Lighthouse Simulated 4G (default). Cold-run (no prior cache).

## NAV-03 — Zero mailto/email in prerendered HTML (non-regression)

- Grep: `grep -r "mailto\|@.*\." .next/server/app --include="*.html" | grep -v twitter | grep -v schema.org`
- Result: {N} matches (expected 0)
- Verdict: **PASS** / FAIL

## A11Y overall (roll-up)

- 06-01 found {X} violations across 8 Spanish pages.
- 06-02 + 06-03 fixed all component-owned + HeroCarousel-owned violations.
- 06-03-SUMMARY.md records post-fix axe = 0 violations on component-owned rules.
- 06-02-SUMMARY.md records carousel a11y upgrade + manual contrast spot-check (all 3 slides ≥ 4.5:1).

## Overall Phase 6 verdict

**PASS** / FAIL

If FAIL: list which requirement failed, why, and what's needed to close the gap (likely a gap-closure plan in a follow-up).

## Deviations / notes

(Anything worth recording — e.g. a manual LCP slightly above 2.5s on first run but stable at second-run, a CLS ghost from an extension, etc.)
```

Commit: `docs(06-04): Phase 6 final verification — all PERF + NAV-03 checks`.
  </action>
  <verify>
1. `test -f .planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md` succeeds.
2. SUMMARY.md contains all required sections (PERF-01, PERF-02/05 CLS table, Contact LCP element, PERF-04 LCP table, NAV-03, A11Y roll-up, Overall verdict).
3. Every PASS / FAIL verdict cell is filled in with numeric evidence, not placeholders.
4. `git log -1 --format=%s` starts with `docs(06-04)`.
  </verify>
  <done>
Phase 6 has a single, comprehensive pass/fail artifact consolidating machine checks + human Lighthouse measurements. The verifier can read this one file to sign off the phase.
  </done>
</task>

</tasks>

<verification>
Overall plan checks:
1. `pnpm build` clean, all routes static.
2. prerender-manifest.json reports ≥ 43 routes, 0 dynamic SSR.
3. NAV-03 grep 0 hits.
4. Lighthouse LCP × 3 pages all < 2.5s (human measured).
5. Lighthouse CLS × 8 pages all = 0 (human measured).
6. Contact page LCP element is not the Maps iframe.
7. 06-04-SUMMARY.md committed as the phase artifact.
</verification>

<success_criteria>
- All five Phase 6 ROADMAP success criteria (axe zero, keyboard/alt, contrast, static, Lighthouse) have documented evidence across 06-01..06-04 SUMMARYs.
- PERF-01 verified via prerender-manifest (machine-readable).
- PERF-02/04/05 verified via Lighthouse (human-driven) with specific numbers.
- NAV-03 verified via grep (carried invariant).
- Phase 6 verdict = PASS in 06-04-SUMMARY.md.
</success_criteria>

<output>
After completion, `.planning/phases/06-polish-a11y-performance/06-04-SUMMARY.md` exists as the Phase 6 final verification artifact. Phase 6 is ready for `/gsd:verify-phase` sign-off.
</output>
