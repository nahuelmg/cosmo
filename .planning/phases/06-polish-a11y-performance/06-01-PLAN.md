---
phase: 06-polish-a11y-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "axe-core CLI runs successfully against all 8 Spanish pages on localhost:3000"
    - "Every axe violation on every audited page is captured in 06-01-SUMMARY.md with rule id, severity, selector, and page URL"
    - "The violation inventory is organized so 06-02 and 06-03 can split remediation work without ambiguity"
  artifacts:
    - path: ".planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md"
      provides: "Ordered violation inventory + axe-core runner record"
      contains: "violations table, per-page status, rule-id column"
  key_links:
    - from: "localhost:3000 (pnpm start on production build)"
      to: "@axe-core/cli@4 via npx"
      via: "CLI invocation with 8 URLs + --load-delay 1500"
    - from: "06-01-SUMMARY.md violations section"
      to: "06-02-PLAN (HeroCarousel rows) + 06-03-PLAN (everything else)"
      via: "clearly labeled partition between carousel-owned and component-owned violations"
---

<objective>
Run @axe-core/cli against the 8 Spanish pages on a local production build and produce a complete, partitioned violation inventory in 06-01-SUMMARY.md. Pure discovery plan — no remediation, no component edits.

Purpose: 06-02 (HeroCarousel a11y upgrade) and 06-03 (axe remediation sweep) are driven by what this plan finds. Without an inventory, remediation is guesswork; with one, the split between the two Wave-2 plans is mechanical.

Output: 06-01-SUMMARY.md listing every violation axe surfaces (rule id, severity, page URL, element selector, short description) plus a partition header separating "HeroCarousel-owned" (fixed in 06-02) from "everything else" (fixed in 06-03).
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-a11y-performance/06-CONTEXT.md
@.planning/phases/06-polish-a11y-performance/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run axe-core CLI against 8 Spanish pages on local production build</name>
  <files>.planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md</files>
  <action>
Produce the raw axe-core violation set for the 8 audited pages.

1. Build + start production server:
   ```bash
   pnpm build
   pnpm start &   # background, capture PID
   ```
   Wait until `http://localhost:3000/es` responds 200 (poll with curl).

2. Run the audit in ONE invocation (per RESEARCH.md — 8 URLs, space-separated, no file-list runner):
   ```bash
   npx -y @axe-core/cli@4 \
     http://localhost:3000/es \
     http://localhost:3000/es/personas \
     http://localhost:3000/es/personas/esteban-calzetta \
     http://localhost:3000/es/investigacion \
     http://localhost:3000/es/publicaciones \
     http://localhost:3000/es/contacto \
     http://localhost:3000/es/divulgacion \
     http://localhost:3000/es/journal-club \
     --load-delay 1500
   ```

   Note: the Spanish route for journal-club is `/journal-club` (not `/club-de-revista`) — confirmed in `src/i18n/routing.ts`. RESEARCH.md example used the wrong slug; use the one in this action.

3. Capture the full terminal output to a scratch variable/file (do not commit the raw output — SUMMARY.md will hold the structured version).

4. Stop the production server (kill the PID you captured).

5. If axe fails to run (Puppeteer browser launch fails, etc.), report the error and halt — this is a hard prerequisite for 06-02/06-03.

Do NOT fix anything in this task. Do NOT edit any component file. Pure discovery.
  </action>
  <verify>
1. The axe command exits (exit code 0 or >0 — both OK; non-zero just means violations were found, which is expected).
2. Terminal output contains a violations section for each of the 8 URLs (or "0 violations" for clean pages).
3. No "Error: Failed to launch browser" or similar infrastructure error.
  </verify>
  <done>
All 8 pages have been scanned. Raw axe output captured in-memory / scratch for Task 2 to structure. Production server stopped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write partitioned violation inventory to 06-01-SUMMARY.md</name>
  <files>.planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md</files>
  <action>
Transform the raw axe output into a structured, actionable inventory. Use this exact SUMMARY.md layout:

```markdown
# Phase 6 · Plan 01 — A11y Audit Inventory

**Completed:** {date}
**Audited:** 8 Spanish pages on localhost:3000 (pnpm build && pnpm start)
**Tool:** @axe-core/cli@4 via npx
**Load delay:** 1500ms (React hydration)

## Per-page violation count

| Page | URL | Critical | Serious | Moderate | Minor | Total |
|------|-----|---------:|--------:|---------:|------:|------:|
| Home | /es | … | … | … | … | … |
| People list | /es/personas | … | … | … | … | … |
| Person detail | /es/personas/esteban-calzetta | … | … | … | … | … |
| Research | /es/investigacion | … | … | … | … | … |
| Publications | /es/publicaciones | … | … | … | … | … |
| Contact | /es/contacto | … | … | … | … | … |
| Outreach | /es/divulgacion | … | … | … | … | … |
| Journal Club | /es/journal-club | … | … | … | … | … |

## Violations — HeroCarousel-owned (06-02 will fix)

Rows where the violation selector is inside `src/components/home/HeroCarousel.tsx` or its descendants. These do NOT belong in 06-03.

| Rule | Severity | Page | Selector | Summary |
|------|----------|------|----------|---------|
| e.g. `aria-allowed-attr` | serious | /es | `[aria-roledescription="carousel"]` | missing aria-label on carousel root |

## Violations — Component-owned (06-03 will fix)

Everything else. Partition by owning file so 06-03 can work component-by-component.

| Rule | Severity | Page | Selector | Owning file | Summary |
|------|----------|------|----------|-------------|---------|
| e.g. `color-contrast` | serious | /es/personas | `.text-ink-subtle` | `src/app/globals.css` (token) | contrast ~3.5:1 < 4.5:1 |
| e.g. `button-name` | serious | all | `.locale-toggle button` | `src/components/layout/LocaleToggle.tsx` | … |

## Rule-family roll-up

Small table grouping violations by rule id → count → recommended fix location. Helps 06-03 see where to concentrate.

| Rule | Total | Owning file(s) | Fix hint |
|------|------:|----------------|----------|
| … | … | … | … |

## Notes

- Anything axe-core could not evaluate (`incomplete`) that might be relevant — list separately.
- If `color-contrast` flags `text-ink-subtle` (RESEARCH.md Open Question #1) — call it out; 06-03 will need a design token edit.
- If `scrollable-region-focusable` fires on the carousel — RESEARCH.md Pitfall #6 says this is a false positive on `overflow: hidden`; document and do NOT escalate.
- If axe surfaces zero violations on a page, note explicitly: "0 violations — page passes." Do not invent phantom items.

## Raw output

(Optional, short.) First 40 lines of the raw axe output pasted in a fenced block if helpful for audit traceability. Do not paste >40 lines.
```

Notes for the executor:
- If axe output has `Incomplete` entries (axe could not determine pass/fail), list them under "Notes" — a human judgment may be needed, but not gating for 06-02/06-03 unless clearly a real issue.
- The partition HeroCarousel-owned / Component-owned is the load-bearing output. Any violation with a selector matching `[aria-roledescription="carousel"]` or descendants of `.relative.w-full.h-\[min\(85svh\,720px\)\]` (the carousel root className prefix) belongs to 06-02. Everything else → 06-03.
- Commit this file with message `docs(06-01): a11y audit inventory`.
  </action>
  <verify>
1. `test -f .planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md` succeeds.
2. The file contains ALL four required sections (per-page count, HeroCarousel-owned, Component-owned, rule-family roll-up).
3. Per-page table has exactly 8 rows, one per audited URL.
4. At least the four section headers exist as plain markdown (not HTML comments).
5. `git log -1 --name-only` shows the commit message starting with `docs(06-01)`.
  </verify>
  <done>
06-01-SUMMARY.md exists, committed, and gives 06-02 + 06-03 executors an unambiguous work list. No component files have been modified in this plan.
  </done>
</task>

</tasks>

<verification>
Overall plan checks:
1. `git status` is clean except for the new SUMMARY.md (which is committed).
2. No files under `src/` were modified.
3. Running `npx @axe-core/cli@4 --help` would succeed (runner available).
4. 06-01-SUMMARY.md's HeroCarousel-owned section gives 06-02 its scope; Component-owned section gives 06-03 its scope.
</verification>

<success_criteria>
- Pure discovery plan: zero source-code edits, zero dependency installs.
- 06-01-SUMMARY.md committed with per-page counts, partitioned violation lists, and a rule-family roll-up.
- 06-02 and 06-03 have a mechanical way to know which violations they own.
</success_criteria>

<output>
After completion, `.planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md` exists, is committed, and contains the four required sections plus notes.
</output>
