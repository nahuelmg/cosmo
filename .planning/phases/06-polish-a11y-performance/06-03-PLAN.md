---
phase: 06-polish-a11y-performance
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/globals.css
  - src/components/layout/LocaleToggle.tsx
  - src/components/layout/NavLink.tsx
  - .planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Every violation in 06-01-SUMMARY.md's 'Component-owned' section is fixed, with the fix site documented per-violation"
    - "Re-running @axe-core/cli against the 8 Spanish pages after the fixes returns zero violations on the rules previously flagged for component-owned code (HeroCarousel-owned rules are still 06-02's scope and may pass or still need work independently)"
    - "No file owned by 06-02 (HeroCarousel.tsx, SiteHeader.tsx, PersonDetail.tsx, messages/*.json) is modified in this plan"
    - "No outline-style borders introduced — all focus rings remain box-shadow via focus-visible:ring-2 (01-02 no-border policy)"
    - "The second axe run is the PASS signal; its output is captured in 06-03-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md"
      provides: "Per-violation fix log + pre/post axe diff + final violations count"
      contains: "per-violation fix, second axe run, delta"
  key_links:
    - from: "06-01-SUMMARY.md 'Component-owned' violation table"
      to: "commits in 06-03"
      via: "one commit per owning-file group (OR one consolidated commit if scope tiny)"
    - from: "post-fix axe run"
      to: "06-03-SUMMARY.md pre/post table"
      via: "count-before vs count-after column"

# Note: files_modified is a SUPERSET guess — the actual file list depends on
# what 06-01 surfaced. Common candidates (in likelihood order from RESEARCH.md):
#   1. src/app/globals.css            — text-ink-subtle token contrast fix (MEDIUM likely)
#   2. src/components/layout/LocaleToggle.tsx  — aria-label refinement (LOW likely)
#   3. src/components/layout/NavLink.tsx       — focus-visible ring addition (LOW likely)
#   4. src/components/contact/ContactDetails.tsx — sr-only label additions (LOW)
#   5. src/components/outreach/OutreachCard.tsx — aria-labelledby on <article> (LOW)
#   6. src/components/publications/PublicationEntry.tsx — duplicate-id check (LOW)
#
# Add or remove entries in frontmatter files_modified as the actual fix list
# dictates — DO NOT touch files owned by 06-02 (HeroCarousel, SiteHeader,
# PersonDetail, messages/*.json) under any circumstance to preserve Wave-2
# parallelism and avoid rebase conflicts.
---

<objective>
Fix every violation axe-core surfaced in 06-01-SUMMARY.md's "Component-owned" section. Prove the fix with a second axe run.

Purpose: Satisfies A11Y-01 (zero critical violations), A11Y-02 (4.5:1 contrast — if `text-ink-subtle` fails, token adjusted), A11Y-03 (keyboard focus indicators — NavLink gap closed if present), A11Y-05 (images alt — any image without proper alt fixed). Consumes 06-01's inventory.

Output: One or more commits that fix the flagged violations, plus 06-03-SUMMARY.md documenting every change + a before/after axe run.

### File-ownership contract (load-bearing for Wave-2 parallelism)

06-02 exclusively owns:
- `src/components/home/HeroCarousel.tsx`
- `src/components/layout/SiteHeader.tsx`
- `src/components/people/PersonDetail.tsx`
- `messages/es.json` + `messages/en.json`

If 06-01 surfaced a violation in one of those files, leave it for 06-02 (it's in 06-01's "HeroCarousel-owned" section already — 06-03 should not find it here). If you inadvertently need a new translation key for a fix in THIS plan, inline the string in the component (see STATE.md decision 03-03: "aria-label strings inlined (not translated)" sets precedent for LocaleToggle). This preserves parallelism with 06-02.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-polish-a11y-performance/06-CONTEXT.md
@.planning/phases/06-polish-a11y-performance/06-RESEARCH.md
@.planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk 06-01's Component-owned violations and fix each one</name>
  <files>(driven by 06-01-SUMMARY.md — see frontmatter files_modified for likely candidates)</files>
  <action>
Open `.planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md`. Work through the "Component-owned" violation table row by row.

For each violation:

1. **Read the selector + owning file.** Open the file. Locate the offending element.
2. **Pick the narrowest fix.** Strict polish bar — fix the actual issue, don't refactor the component.
3. **Apply the fix.** Common patterns (all from RESEARCH.md):

   | Rule | Typical fix |
   |------|-------------|
   | `color-contrast` on `.text-ink-subtle` | In `src/app/globals.css`, darken `--color-ink-subtle` from `oklch(0.62 …)` to `oklch(0.45 …)` or lower. This is a one-token change affecting every usage site. Verify the component-level visual hasn't regressed on 2-3 representative pages (ContactDetails small-caps labels, OutreachCard date line). |
   | `color-contrast` on `.text-ink-muted` | Similarly tune `--color-ink-muted` if axe flags it |
   | `button-name` or `link-name` on LocaleToggle | Already has inline Spanish/English aria-label per STATE.md 03-03 — if axe flags it, verify the label is actually reaching the DOM (Suspense fallback null might be why — but the inner renders the label). Add `aria-label` to outer Suspense wrapper if still flagged. |
   | `focusable-content` or missing focus on NavLink | Add `focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:rounded focus-visible:outline-none` to the anchor's className (same tokens the rest of the codebase uses — 01-02 no-border policy) |
   | `region` on `<article>` or `<section>` without accessible name | Add `aria-labelledby={id}` pointing at the heading inside the section, OR `aria-label` inline |
   | `duplicate-id` on `pub-${publication.id}` | Verify `publication.id` is genuinely unique across years; if not, prefix with year (`pub-${year}-${id}`) and update any fragment links |
   | `image-alt` | Add meaningful `alt=` (per RESEARCH.md "alt text decision tree") or empty `alt=""` for decorative |
   | `landmark-unique` | If multiple `<main>` or `<nav>` landmarks exist, ensure each has a distinguishing aria-label OR collapse to one |

4. **Guards while fixing:**
   - NEVER modify files owned by 06-02 (HeroCarousel, SiteHeader, PersonDetail, messages/*.json). If a violation there was mis-partitioned, note it in 06-03-SUMMARY.md and escalate — do not cross the file-ownership line.
   - NEVER add `outline:` styles to fix focus. Use `focus-visible:ring-2 focus-visible:ring-accent-ring` box-shadow pattern (01-02).
   - NEVER add text-shadow to hide contrast issues. Fix the token.
   - NEVER add `role="button"` to a `<div>` — use `<button>`.

5. **After each file's fixes:** run `pnpm typecheck` to catch type breakage immediately.

6. **Before committing, run `pnpm build`** — the full build must still prerender all 43 pages static (PERF-01 non-regression).

7. **Commit strategy:**
   - If fixes are all in 1-2 files → single commit: `fix(06-03): close {N} axe violations — {rule-ids summary}`
   - If fixes span 3+ files → one commit per owning file, message: `fix(06-03): {file basename} — {rule summary}`

Update `files_modified` in THIS plan's frontmatter (retroactively via editing 06-03-PLAN.md) if the actual file set differs from the prediction. This keeps verification-phase tooling accurate.

### If 06-01-SUMMARY.md shows ZERO component-owned violations

Possible outcome — 06-02 absorbs everything. In that case:
- Write 06-03-SUMMARY.md stating "06-01 found 0 component-owned violations; no code changes in this plan."
- Still run the Task 2 second-axe validation (it's the authoritative signal).
- Skip commit in this task, proceed to Task 2.
  </action>
  <verify>
1. `pnpm typecheck` exits 0.
2. `pnpm build` exits 0 and the terminal table shows all content pages as `○` (static). Count: 45 static routes total (43 content + _not-found + _global-error) per RESEARCH.md Pattern 4.
3. For each violation in 06-01-SUMMARY.md's Component-owned table: the owning file now contains the fix (grep for the specific selector/class/token).
4. `git log --since="today" --oneline | grep "fix(06-03)"` shows the commit(s).
5. No file listed under 06-02's files_modified has been modified in this plan: `git diff --name-only HEAD~N HEAD` for this plan's commits does NOT include HeroCarousel.tsx, SiteHeader.tsx, PersonDetail.tsx, or messages/*.json.
  </verify>
  <done>
Every component-owned violation flagged in 06-01 has a targeted fix; typecheck + build clean; 06-02's file set is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run @axe-core/cli to confirm fixes + write 06-03-SUMMARY.md</name>
  <files>.planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md</files>
  <action>
Validate the fixes with a second axe run. This is the authoritative pass signal.

1. `pnpm build && pnpm start` (background, capture PID).

2. Wait for server ready, then run the same axe command as 06-01:
   ```bash
   npx -y @axe-core/cli@4 \
     http://localhost:3000/es \
     http://localhost:3000/es/personas \
     http://localhost:3000/es/personas/esteban-calzetta \
     http://localhost:3000/es/investigacion \
     http://localhost:3000/es/publicaciones \
     http://localhost:3000/es/contacto \
     http://localhost:3000/es/divulgacion \
     http://localhost:3000/es/journal-club \
     --load-delay 1500
   ```

3. Kill the server. Record the per-page violation counts AFTER fixes.

4. Write 06-03-SUMMARY.md:

```markdown
# Phase 6 · Plan 03 — Axe Remediation Sweep

**Completed:** {date}
**Source inventory:** .planning/phases/06-polish-a11y-performance/06-01-SUMMARY.md

## Per-violation fix log

| # | Rule | Page | Selector | Fix site | Commit |
|--:|------|------|----------|----------|--------|
| 1 | color-contrast | /es/personas | .text-ink-subtle | src/app/globals.css — --color-ink-subtle → oklch(0.45 …) | fix(06-03): … |
| 2 | … | … | … | … | … |

## Pre → post axe counts

| Page | Before (06-01) | After (06-03) | Delta |
|------|---:|---:|---:|
| /es | X | 0 | -X |
| /es/personas | X | 0 | -X |
| … | … | … | … |

**Expectation:** all Component-owned violations → 0 after 06-03. HeroCarousel-owned violations are 06-02's responsibility; their delta depends on 06-02 completing independently. If 06-02 has not merged yet at the time of THIS re-run, note the HeroCarousel-owned violations are out of this plan's scope and point to 06-02-SUMMARY.md for their resolution.

## No-regression checks

- `pnpm build` prerendered 45 static routes (0 dynamic). PERF-01 held.
- `grep -r "mailto\|@.*\." .next/server/app --include="*.html" | grep -v twitter | grep -v schema.org` returns 0 lines. NAV-03 held.
- No new outline-style borders introduced.
- No files owned by 06-02 modified in this plan.

## Deviations / notes

(If any — e.g. a violation that was out of scope and escalated to 06-02, any axe false positives per RESEARCH.md Pitfall #6, any test-ink-subtle downstream visual surprise.)
```

5. Commit: `docs(06-03): axe remediation sweep + post-fix verification`.
  </action>
  <verify>
1. `test -f .planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md` succeeds.
2. SUMMARY.md contains the three required sections (Per-violation fix log, Pre → post axe counts, No-regression checks).
3. Pre/post table has exactly 8 page rows and an "After" column with numeric values (0 is the target for component-owned violations).
4. NAV-03 grep documented as 0 hits; PERF-01 documented as all routes static.
5. `git log -1 --format=%s` starts with `docs(06-03)`.
  </verify>
  <done>
Second axe run confirms component-owned violations are resolved; evidence committed in 06-03-SUMMARY.md alongside NAV-03 + PERF-01 non-regression checks.
  </done>
</task>

</tasks>

<verification>
Overall plan checks:
1. `pnpm typecheck && pnpm build` clean after all commits.
2. Post-fix axe run (Task 2) shows all previously flagged Component-owned rules returning 0 violations on every page.
3. `git diff --name-only <before-this-plan> HEAD` does NOT include HeroCarousel.tsx, SiteHeader.tsx, PersonDetail.tsx, messages/es.json, messages/en.json (those are 06-02's file set).
4. All 45 static routes still prerender (PERF-01 non-regression).
5. NAV-03 grep still returns 0 mailto/email hits in prerendered HTML.
</verification>

<success_criteria>
- Every component-owned violation from 06-01 has a targeted fix with a per-violation row in 06-03-SUMMARY.md's fix log.
- Second axe run shows 0 violations on the component-owned rule set.
- 06-02's file ownership respected — zero edits to its files.
- No outline borders introduced; no text-shadow hiding contrast failures; no mailto regression.
</success_criteria>

<output>
After completion, `.planning/phases/06-polish-a11y-performance/06-03-SUMMARY.md` exists with fix log, pre/post counts, and non-regression evidence. If actual file list differed from the prediction in frontmatter, the `files_modified` field has been updated retroactively.
</output>
