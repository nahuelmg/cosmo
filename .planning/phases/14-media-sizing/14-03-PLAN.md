---
phase: 14-media-sizing
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "HeroCarousel reads cleanly at 375 / 768 / 1024 / 1440 / 1920 px — no cropped faces, no awkward H1 wrap, no scrim text truncation"
    - "OutreachCard photos read balanced against their text block on /outreach at 1024 / 1440 / 1920 px"
    - "Homepage imagery inventory confirmed: ONLY HeroCarousel carries images (no News/Research/Highlights raster media exists)"
    - "No regressions introduced by plans 14-01 and 14-02 at any measured viewport"
    - "Combined srcset audit confirms no oversized downloads across /, /people, /people/[slug], /outreach"
    - "CLS stays at 0 across all four audited pages"
    - "axe-core reports zero violations on the Spanish pages that changed (/, /personas, /personas/[slug], /outreach)"
  artifacts:
    - path: ".planning/phases/14-media-sizing/14-03-SUMMARY.md"
      provides: "Audit findings for MEDIA-03, MEDIA-04 + cross-phase verification report"
      contains: "HeroCarousel"
  key_links: []
---

<objective>
Audit HeroCarousel (MEDIA-03), OutreachCard + homepage highlight imagery (MEDIA-04), and run a combined verification pass across all four pages touched or adjacent to this phase (/, /people, /people/[slug], /outreach). Close MEDIA-03 and MEDIA-04. Confirm no regressions from plans 14-01 and 14-02. Ship the phase.

Purpose: CONTEXT scopes HeroCarousel and OutreachCard to AUDIT ONLY — do not pre-emptively resize. MEDIA-04 also collapses to auditing HeroCarousel after research confirmed the homepage carries no other `next/image` surfaces (Highlights, PartnerStrip, ResearchCard are text/icon-only). This plan documents the current state at the four required viewport widths (375/768/1024/1440 — plus 1920 for wide screens), flags any concrete regressions for follow-up, and verifies the 14-01 and 14-02 changes didn't break anything.

Output:
- Written audit with findings per viewport for HeroCarousel, OutreachCard, homepage
- Combined regression check confirming PersonCard grid + PersonDetail hero still render correctly
- axe-core report on changed Spanish pages
- Build gate green
- Phase-level SUMMARY recording all five MEDIA requirements closed (or explicitly deferred with reason)
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-media-sizing/14-CONTEXT.md
@.planning/phases/14-media-sizing/14-RESEARCH.md

# Prior-plan outputs this audit verifies against:
@.planning/phases/14-media-sizing/14-01-SUMMARY.md
@.planning/phases/14-media-sizing/14-02-SUMMARY.md

# Components under audit (read-only):
@src/components/home/HeroCarousel.tsx
@src/components/home/Highlights.tsx
@src/components/home/PartnerStrip.tsx
@src/components/outreach/OutreachCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroCarousel audit at 5 viewports (MEDIA-03)</name>
  <files>(no files modified — audit only)</files>
  <action>
Start the dev server. Navigate to `http://localhost:3000/es` (homepage in Spanish, the canonical locale). For each of the 5 viewport widths — 375, 768, 1024, 1440, 1920 px — evaluate the HeroCarousel against the audit checklist below.

**Files under audit (read-only):**
- `src/components/home/HeroCarousel.tsx` — slide wrapper at line 96: `h-[min(85svh,720px)] md:h-auto md:aspect-[21/9]`, Image at lines 121–136 uses `sizes="100vw"`.

**Audit checklist per viewport:**

For each width, record PASS / FAIL (with notes) against:

1. **H1 text wrap**: the Spanish title "Grupo de Cosmología" (or current content) does not wrap awkwardly, does not disappear, does not collide with the image edges.
2. **Scrim text truncation**: the gradient scrim at the bottom of the hero does not cut off subtitle text or carousel dots.
3. **Face/subject cropping**: the slide photo does not crop a face or the central subject because `object-cover` chose an awkward crop frame at this aspect ratio.
4. **Control reachability**: prev/next arrows and dot indicators are visible AND tappable (≥ 44 × 44 tap area — BTN-01 is Phase 15, but pre-existing regressions should be flagged).
5. **min-height behavior**: on mobile (< md), the `h-[min(85svh,720px)]` clamp keeps the hero readable; on md+, `aspect-[21/9]` keeps the hero cinematic-wide.
6. **LCP preload**: inspect the HTML (`curl http://localhost:3000/es | grep 'rel="preload"'`) and confirm the first slide image has a `<link rel="preload" imagesrcset=...>` entry. (Phase 6 invariant — should still be there.)

**Per CONTEXT.md decision: NO PRESCRIPTIVE RESIZE.** If ALL six checks pass at all five viewports, the audit closes MEDIA-03 with no code change. If ANY check fails at ANY viewport, record the exact failure (viewport + which check + symptom) in the SUMMARY as a "MEDIA-03 audit finding" and file it as a deferred follow-up — do NOT fix HeroCarousel in this phase (research Pitfall 4: scrim gradient, text shadows, min-height were Phase 6-tuned; touching the aspect cascades).

**Expected baseline (research):**
- 375 px: clamped height (≤ 85svh or 720 px, whichever smaller)
- 768 px+: 21:9 cinematic aspect
- All five widths: LCP preload link present, srcset candidate from `deviceSizes` pool, CLS = 0

Take a screenshot of each viewport if possible (saved to `/tmp/14-03-hero-{viewport}.png` or equivalent) so the audit is reviewable. Screenshots are optional if time-pressed; the written checklist is the load-bearing deliverable.

Do NOT:
- Edit `HeroCarousel.tsx` under any circumstances in this plan. If a fix is needed, file it as a new follow-up plan, not an edit here (CONTEXT + research Pitfall 4).
- Touch Phase 6 LCP props (`preload`, `loading`, `fetchPriority`).
- Run Lighthouse in a way that mutates content (read-only audit only).
  </action>
  <verify>
- SUMMARY includes a 5-row × 6-check table for HeroCarousel audit
- Any failures are specific (viewport + check + symptom) — no "feels off" hand-waves
- No modifications to `src/components/home/HeroCarousel.tsx` (`git diff --stat src/components/home/HeroCarousel.tsx` returns nothing)
- Preload `<link>` confirmed present in rendered HTML
  </verify>
  <done>
- 5×6 audit table completed with PASS/FAIL per cell
- MEDIA-03 marked closed (if all PASS) OR MEDIA-03 marked "audit findings filed for follow-up" with specifics (if any FAIL)
- Zero code changes to HeroCarousel.tsx verified via git diff
  </done>
</task>

<task type="auto">
  <name>Task 2: OutreachCard + homepage imagery audit (MEDIA-04)</name>
  <files>(no files modified — audit only)</files>
  <action>
Complete the MEDIA-04 audit. Research already confirmed the homepage has ONLY the HeroCarousel image surface (Highlights, PartnerStrip, ResearchCard are text/icon-only — see research "Homepage imagery inventory" table). So MEDIA-04 collapses to: (a) restate the inventory finding, and (b) audit OutreachCard.

**Part A — Homepage imagery inventory confirmation:**

Grep the homepage tree to reconfirm no new `next/image` usages have slipped in since research:

```
grep -rn "from 'next/image'" src/app/\[locale\]/page.tsx src/components/home/ | sort
```

Expected matches: HeroCarousel.tsx only. If `Highlights.tsx`, `PartnerStrip.tsx`, or any new home/* file imports `next/image`, that is a finding — record it and evaluate against the warm-academic "image supports, text leads" rule. If only HeroCarousel matches, record: "Homepage imagery inventory confirmed: HeroCarousel is the only image surface. MEDIA-04 therefore closes via MEDIA-03 audit + OutreachCard audit."

**Part B — OutreachCard audit at 3 viewports:**

Navigate to `http://localhost:3000/es/divulgacion` (Spanish outreach page — confirm the exact slug via `src/i18n/routing.ts` if unsure; /outreach maps to /divulgacion in ES).

At 1024, 1440, 1920 px (skip mobile — research noted the grid is identical under 1024 and the 3-col comparison is the dominant-vs-supporting test):

1. Visually compare card photo area vs card text area. Warm-academic test (CONTEXT "image supports, text leads"): if the 16:9 photo visibly outweighs the adjacent headline + description block, that's "over-dominant". If it feels balanced (photo as supporting context), it's fine.
2. DevTools-inspect one OutreachCard: confirm wrapper class `aspect-[16/9]`, `sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw"`, Image uses `fill` + `object-cover`. These should match research expectations (OutreachCard.tsx:26,31).
3. Network tab: reload, filter images, confirm OutreachCard photo `?w=` values resolve from the `deviceSizes` pool — at lg+ 3-col grid, 33vw of 1440 = 475 px → srcset picks 640 px at 1× DPR, 1080 px at 2× DPR. These are acceptable at 16:9 full-width-of-cell (unlike PersonCard's narrow 240 px cap, OutreachCard genuinely renders near its advertised width).

**Decision per CONTEXT:** AUDIT ONLY. If all three viewports pass the balance test, MEDIA-04 closes no code change. If the 1024 or 1440 viewport shows "over-dominant" photo, file a follow-up recommendation (crop source images or switch to `aspect-[3/2]`) but DO NOT execute the fix in this phase — research Pitfall 6 ("fix is to crop the source images or switch aspect-ratio, but only after the audit confirms a concrete issue").

Do NOT:
- Edit `OutreachCard.tsx`, `OutreachGrid.tsx`, or any outreach file.
- Edit any homepage component.
- Crop or regenerate source image files in `public/`.
  </action>
  <verify>
- `grep -rn "from 'next/image'" src/app/\[locale\]/page.tsx src/components/home/` output recorded in SUMMARY; HeroCarousel is the only match
- SUMMARY includes a 3-row audit table for OutreachCard (1024/1440/1920 × balance-test finding)
- `git diff --stat src/components/outreach/ src/components/home/` returns nothing (no modifications)
- Any "over-dominant" findings filed as follow-up with the recommended fix (crop / aspect change) — not executed here
  </verify>
  <done>
- Homepage imagery inventory reconfirmed (research finding holds)
- OutreachCard 3-viewport audit documented
- MEDIA-04 marked closed (if all PASS) OR marked "audit findings filed for follow-up" with specifics
- No source files modified in this task
  </done>
</task>

<task type="auto">
  <name>Task 3: Combined regression + axe verification + phase close</name>
  <files>(no files modified — verification + SUMMARY)</files>
  <action>
Final verification sweep combining plan 14-01 + 14-02 changes with this plan's audit work.

1. **Build gate**:
   - `pnpm tsc --noEmit` passes
   - `pnpm build` passes
   - `pnpm test` passes (if any exist for components touched)

2. **Full-page revisit at 5 viewports**. With dev server running, revisit each of these four pages at 375 / 768 / 1024 / 1440 / 1920 px and record PASS/FAIL for "no visible regression":
   - `/es` — homepage (includes HeroCarousel; audited in Task 1)
   - `/es/personas` — PeopleSection grid (spot-check: 240 px cards at lg+, 4-col at xl)
   - `/es/personas/<slug>` — PersonDetail hero (spot-check: 180 px photo at md+, LCP preload link present)
   - `/es/divulgacion` — OutreachCard grid (audited in Task 2)

   If 14-01's measurements and 14-02's measurements still hold, this is a quick re-confirmation. Record any drift from their SUMMARY findings.

3. **EN spot-check**: at 1024 px, load `/en`, `/en/people`, `/en/people/<slug>`, `/en/outreach`. Confirm:
   - PersonCard role strings don't wrap awkwardly (if they do, note as content follow-up — same as 14-01 Task 3).
   - PersonDetail bio reflow in English looks natural at the wider bio column.
   - No layout differences beyond translated copy.

4. **axe-core verification** (manual DevTools is the primary path — there is no `pnpm axe` script in this repo):

   Run axe manually via the browser DevTools axe extension on each of the four changed Spanish pages:
   - `/es`
   - `/es/personas`
   - `/es/personas/<slug>` (pick any member slug from the directory)
   - `/es/divulgacion`

   Steps per page: open DevTools → axe DevTools panel → "Scan ALL of my page" → record the violation count. Expected: 0 violations on all four pages (Phase 6 + Phase 13 baseline). Any new violation is a blocker and must be investigated before closing the phase — record the rule ID, node selector, and impact level in the SUMMARY.

   (If a `pnpm axe` script is ever added to `package.json`, use `pnpm axe` instead — it will cover the same pages programmatically. Verify first with `grep -E '"axe"' package.json`; skip the invocation if the script is absent.)

5. **CLS sweep**: Performance tab → Reload on each of the four pages at 1024 px. Record CLS for each. All must be 0 (or unchanged from Phase 6 baseline).

6. **Srcset byte-saving confirmation**: Network tab → disable cache → reload `/es/personas`. Record total bytes transferred for "Img" filter. Compare to pre-phase baseline if one was captured; expected reduction is ~40–60 % for the images subtotal (main person grid was serving 1080 px candidates for 340 px render widths — now serving 256–640 px for 240 px renders).
   
   If no baseline was captured pre-phase, just record the post-phase number for future reference.

7. **Write phase-level SUMMARY** at `.planning/phases/14-media-sizing/14-03-SUMMARY.md` consolidating:
   - Task 1 HeroCarousel audit table
   - Task 2 OutreachCard audit table + homepage imagery inventory confirmation
   - Task 3 regression sweep results (four pages × five viewports)
   - axe-core violation count per page
   - CLS per page
   - Srcset byte totals
   - Per-requirement closure status:
     - MEDIA-01: closed by 14-01 (ref 14-01-SUMMARY.md) — re-verified here
     - MEDIA-02: closed by 14-02 (ref 14-02-SUMMARY.md) — re-verified here
     - MEDIA-03: closed by Task 1 audit (or: audit findings filed as follow-up)
     - MEDIA-04: closed by Task 2 audit (or: audit findings filed as follow-up)
     - MEDIA-05: closed by 14-01 + 14-02 sizes-attribute edits — re-verified here via Network-tab audit
   - Any open follow-ups (HeroCarousel issues, OutreachCard crops, EN role wrapping) clearly listed with rationale for deferral

Do NOT:
- Edit any source file in this task — this is a verification + writing task only.
- Commit during the task — the phase commit is handled at the end by the execute-phase workflow.
- Skip axe — it's the v1.2 quality gate (REQUIREMENTS.md quality bar).
  </action>
  <verify>
- `pnpm build` exits 0
- `pnpm tsc --noEmit` exits 0
- Manual axe DevTools scan reports 0 violations on each of /es, /es/personas, /es/personas/[slug], /es/divulgacion
- CLS = 0 (or unchanged from baseline) on /es, /es/personas, /es/personas/[slug], /es/divulgacion at 1024 px
- Phase SUMMARY at `.planning/phases/14-media-sizing/14-03-SUMMARY.md` exists and is > 100 lines
- SUMMARY documents closure status for MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05 explicitly
- SUMMARY references 14-01-SUMMARY.md and 14-02-SUMMARY.md for MEDIA-01 + MEDIA-02 + MEDIA-05 details (no duplication)
  </verify>
  <done>
- Build gate green across tsc + build + test
- axe-core reports 0 violations on all audited Spanish pages (via manual DevTools scan)
- CLS = 0 on all four pages at 1024 px
- 14-03-SUMMARY.md captures: HeroCarousel audit table, OutreachCard audit table, homepage inventory confirmation, regression sweep, srcset byte totals, axe results, per-requirement closure status
- All five MEDIA requirements have explicit closure or deferred-with-specifics status
- Phase 14 ready for `/gsd:verify-phase 14`
  </done>
</task>

</tasks>

<verification>
Phase-level gates this plan enforces:

- **MEDIA-03** (HeroCarousel audit at 375/768/1024/1440): CLOSED by Task 1 audit (or deferred with specifics).
- **MEDIA-04** (homepage + OutreachCard audit): CLOSED by Task 2 (inventory confirmation + OutreachCard 3-viewport audit).
- **Regressions from 14-01 and 14-02**: Re-verified in Task 3 across /, /people, /people/[slug], /outreach.
- **v1.2 quality bar** (REQUIREMENTS.md): `pnpm build` green, manual axe DevTools scan zero violations, CLS unchanged.
- **Phase closure**: SUMMARY consolidates all five MEDIA requirements with pointers to earlier plan SUMMARYs where appropriate (no duplication).
</verification>

<success_criteria>
1. `git diff --stat src/` (on the dev branch at plan start vs end) shows NO modifications from this plan — it is audit + verification only
2. `pnpm build` + `pnpm tsc --noEmit` both exit 0
3. Manual axe DevTools scan reports 0 violations on /es, /es/personas, /es/personas/[slug], /es/divulgacion
4. CLS = 0 recorded for all four Spanish pages at 1024 px
5. 14-03-SUMMARY.md exists and includes: HeroCarousel 5×6 audit table, OutreachCard 3-row audit table, homepage inventory grep output, regression sweep table (4 pages × 5 viewports), per-requirement closure status for MEDIA-01..05
6. MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05 each have an explicit "closed" or "audit findings filed for follow-up" status
7. All deferred follow-ups (HeroCarousel regressions, OutreachCard crops, EN content wrapping) recorded with concrete symptoms + recommended fixes (not executed)
</success_criteria>

<output>
After completion, create `.planning/phases/14-media-sizing/14-03-SUMMARY.md` documenting:
- MEDIA-03: HeroCarousel audit at 5 viewports × 6 checks — PASS/FAIL table with notes
- MEDIA-04: homepage imagery inventory grep output confirming HeroCarousel is the only image surface; OutreachCard 3-viewport balance audit
- Combined regression sweep: 4 pages × 5 viewports PASS/FAIL
- axe-core violation counts per changed Spanish page (expected: 0)
- CLS per page at 1024 px (expected: 0)
- Srcset byte totals on /es/personas (post-phase baseline)
- EN spot-check notes (any role-wrap issues, bio reflow findings)
- Per-requirement closure status:
  - MEDIA-01 → closed (14-01)
  - MEDIA-02 → closed (14-02)
  - MEDIA-03 → closed (this plan Task 1) OR deferred with specifics
  - MEDIA-04 → closed (this plan Task 2) OR deferred with specifics
  - MEDIA-05 → closed (14-01 + 14-02)
- Follow-ups filed (if any), with recommended plan/phase for resolution
</output>
