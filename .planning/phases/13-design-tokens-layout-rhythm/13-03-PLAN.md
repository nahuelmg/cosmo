---
phase: 13-design-tokens-layout-rhythm
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/components/research/ResearchCard.tsx
autonomous: true

must_haves:
  truths:
    - "ResearchCard renders with p-6 (spacious tier), not p-8"
    - "PersonCard still uses p-4 (dense tier) — verified, unchanged"
    - "OutreachCard still uses p-6 (spacious tier) — verified, unchanged"
    - "SessionRow still uses py-5 (dense row spacing) — verified, unchanged"
    - "pnpm build passes after the padding change"
  artifacts:
    - path: "src/components/research/ResearchCard.tsx"
      provides: "ResearchCard with two-tier-compliant padding (p-6)"
      contains: "p-6"
  key_links:
    - from: "ResearchCard className"
      to: "Card padding two-tier convention (SPACE-03)"
      via: "Tailwind p-6 utility"
      pattern: "p-6"
---

<objective>
Apply the card-padding two-tier rule (SPACE-03) by reducing ResearchCard from `p-8` to `p-6`. Audit the other three candidate components (PersonCard, OutreachCard, SessionRow) to confirm they already comply — no changes needed.

This plan runs in parallel with Plan 02 (Wave 2) — the card components touched here are disjoint from pages and layout chrome.

Purpose: Completes SPACE-03. Card density now reads: grid cards (people) are dense `p-4`, feature cards (research, outreach) are spacious `p-6`, flat across breakpoints.

Output: Single-file atomic change. Commit: `feat(13): reduce ResearchCard padding to p-6 per SPACE-03`.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-design-tokens-layout-rhythm/13-CONTEXT.md
@.planning/phases/13-design-tokens-layout-rhythm/13-RESEARCH.md
@src/components/research/ResearchCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce ResearchCard padding p-8 → p-6</name>
  <files>src/components/research/ResearchCard.tsx</files>
  <action>
Open `src/components/research/ResearchCard.tsx` and locate the outer card wrapper (line ~21 per RESEARCH.md section 5). Change the className:

- Before: `... p-8 ...` (alongside existing bg/rounded/shadow/etc. classes)
- After:  `... p-6 ...`

Rules:
1. Change only `p-8` → `p-6`. Do NOT modify any other utility class (surface colors, shadows, rounded corners, hover states, transitions).
2. Do NOT add responsive padding (`md:p-6` etc.) — CONTEXT.md locks card padding as flat across breakpoints.
3. Do NOT introduce a new CSS variable or utility wrapper (e.g., `.card-spacious`) — CONTEXT.md explicitly declines that codification.

Verify via a quick read of surrounding code that `p-8` appears exactly once on the card wrapper; if there are additional `p-*` variants anywhere (e.g., inner meta row), leave those alone unless they are directly on the outermost card element.
  </action>
  <verify>
```bash
grep -n "p-8\|p-6" src/components/research/ResearchCard.tsx
# Expected: `p-6` on the outer wrapper; no `p-8` occurrences.

grep -rn "p-8" src/components/research/
# Expected: zero matches (or only in non-card layout contexts).
```

`pnpm build` passes. Visual check at 1024 / 1440 px on `/research`: cards visibly tighter than before (40 px padding → 24 px padding) but still have breathing room; text not crowded against card edges.
  </verify>
  <done>
ResearchCard wrapper uses `p-6`. `grep "p-8" src/components/research/` returns zero. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit PersonCard, OutreachCard, SessionRow for SPACE-03 compliance</name>
  <files>
src/components/people/PersonCard.tsx
src/components/outreach/OutreachCard.tsx
src/components/journal-club/SessionRow.tsx
  </files>
  <action>
**Read-only audit.** For each file, open and confirm (do NOT modify):

1. `src/components/people/PersonCard.tsx` line ~39: outer card wrapper uses `p-4` (dense tier). Expected: already correct.
2. `src/components/outreach/OutreachCard.tsx` line ~36: outer card wrapper uses `p-6` (spacious tier). Expected: already correct.
3. `src/components/journal-club/SessionRow.tsx` line ~29: row wrapper uses `py-5` (dense row spacing — rows use `py-*` not `p-*`; `py-5` is the dense equivalent). Expected: already correct.

If any of these diverge from the expected value, record the deviation in the SUMMARY but do NOT change it — CONTEXT.md locks these as the dense / spacious tier exemplars, so the code is the source of truth for what the tier names mean. A mismatch would be flagged for human review, not auto-fixed.

Do NOT edit these files unless the audit reveals a genuine violation (none expected per RESEARCH.md section 5).
  </action>
  <verify>
```bash
grep -n "p-4\|p-6\|py-5" src/components/people/PersonCard.tsx src/components/outreach/OutreachCard.tsx src/components/journal-club/SessionRow.tsx
# Expected:
# PersonCard → p-4
# OutreachCard → p-6
# SessionRow → py-5
```

No file edits required. Record findings in SUMMARY.
  </verify>
  <done>
Three components audited. All compliant with SPACE-03 two-tier rule. Findings recorded in SUMMARY.
  </done>
</task>

</tasks>

<verification>
Final phase-03 checks:
- `pnpm build` passes.
- `grep -rn "p-8" src/components/research/` → zero matches.
- ResearchCard renders visibly tighter on `/research` (verified in devtools).
- Audit findings documented.
</verification>

<success_criteria>
- ResearchCard wrapper is `p-6` (not `p-8`).
- PersonCard / OutreachCard / SessionRow audited; no changes required.
- `pnpm build` passes.
- Commit: `feat(13): reduce ResearchCard padding to p-6 per SPACE-03`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-tokens-layout-rhythm/13-03-SUMMARY.md` with:
- The one-line ResearchCard change
- Audit results for PersonCard / OutreachCard / SessionRow
- Build result
</output>
