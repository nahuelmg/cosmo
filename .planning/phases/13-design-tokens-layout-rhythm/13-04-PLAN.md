---
phase: 13-design-tokens-layout-rhythm
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - design-system/cosmology-group-uba/MASTER.md
autonomous: true

must_haves:
  truths:
    - "MASTER.md Type Scale table reflects --text-4xl at 36px and includes --text-5xl at 40px"
    - "MASTER.md contains a Layout subsection documenting container widths, vertical rhythm, and card padding tiers"
    - "MASTER.md records the two-tier line-height convention (display leading-tight, body leading-relaxed)"
    - "The v1.0 type scale is clearly marked as superseded so no-one follows the stale table"
  artifacts:
    - path: "design-system/cosmology-group-uba/MASTER.md"
      provides: "Canonical documentation of v1.2 tokens + layout conventions"
      contains: "--text-5xl"
  key_links:
    - from: "MASTER.md Type Scale table"
      to: "src/app/globals.css @theme tokens"
      via: "token names + values"
      pattern: "--text-5xl.*2\\.5rem"
---

<objective>
Update `design-system/cosmology-group-uba/MASTER.md` to reflect the v1.2 tokens and layout conventions shipped by Plans 01–03:

1. **Type Scale table** — update `--text-4xl` row to 2.25rem / 36 px; add a new `--text-5xl` row at 2.5rem / 40 px; mark the v1.0 32 px scale superseded.
2. **New Layout subsection** — document container widths (SPACE-01), vertical rhythm (SPACE-02), and card padding tiers (SPACE-03).
3. **Line-height convention** — record the two-tier rule: display (text-3xl+) → `leading-tight` (1.2); body (text-xl and below) → `leading-relaxed` (1.625).

Purpose: Covers TYPO-05 and SPACE-04. This is the canonical design-system record; without it the token/layout decisions live only in CONTEXT.md (a planning artifact), and future contributors have no single source of truth. Plan 04 is the final Phase-13 artifact.

Output: Markdown-only edits. Single commit: `docs(13): document v1.2 type scale, layout widths, rhythm, and card padding`.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-design-tokens-layout-rhythm/13-CONTEXT.md
@.planning/phases/13-design-tokens-layout-rhythm/13-RESEARCH.md
@.planning/phases/13-design-tokens-layout-rhythm/13-01-SUMMARY.md
@.planning/phases/13-design-tokens-layout-rhythm/13-02-SUMMARY.md
@.planning/phases/13-design-tokens-layout-rhythm/13-03-SUMMARY.md
@design-system/cosmology-group-uba/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Type Scale table with v1.2 values</name>
  <files>design-system/cosmology-group-uba/MASTER.md</files>
  <action>
Locate the "### Type Scale (ratio 1.2)" section (around line 41). The table currently ends at `--text-4xl | 2rem | 32px | H1`.

Apply these edits:

1. Update the `--text-4xl` row:
   - Before: `| --text-4xl | 2rem | 32px | H1 (serif, semibold 600) |`
   - After:  `| --text-4xl | 2.25rem | 36px | H1 on inner pages (serif, semibold 600) |`

2. Add a new row below `--text-4xl`:
   - `| --text-5xl | 2.5rem | 40px | Hero H1 (HomePage HeroCarousel only, serif, semibold 600) |`

3. Immediately after the table, add a short note:
   ```
   > **v1.2 update:** `--text-4xl` bumped from 32 px → 36 px and `--text-5xl` added at 40 px so H1 (36 px) sits ≥1.20× above H2 (30 px). The v1.0 scale entries above this line (`--text-xs` through `--text-3xl`) are unchanged. Any external doc or branch referring to `--text-4xl: 2rem` is superseded.
   ```

4. Add a new subsection immediately after the Type Scale table and note, titled `### Line-Height Convention`:

```
### Line-Height Convention

Two-tier convention per v1.2 Phase 13:

| Tier | Applies to | Value | Tailwind utility |
|------|-----------|-------|------------------|
| Display | `text-3xl` and larger (H1, large H2) | `1.2` | `leading-tight` |
| Body | `text-xl` and smaller (paragraph, list items, card copy) | `1.625` | `leading-relaxed` |

- `h1` is encoded in `@layer base` as `line-height: 1.2`. H2 elements that render at `text-3xl` add `leading-tight` per-component.
- Body prose converges on `leading-relaxed` (1.625). Existing `leading-normal` usage is migrated case-by-case; no blanket `@layer base` body override.
- Letter-spacing unchanged: `-0.01em` on headings via `@layer base`; body stays at 0.
```
  </action>
  <verify>
```bash
grep -n "text-4xl\|text-5xl" design-system/cosmology-group-uba/MASTER.md
# Expected: --text-4xl row shows 2.25rem / 36px; --text-5xl row exists with 2.5rem / 40px.

grep -n "Line-Height Convention" design-system/cosmology-group-uba/MASTER.md
# Expected: one match for the new subsection header.
```
  </verify>
  <done>
Type Scale table reflects v1.2 values. Supersession note added. Line-Height Convention subsection inserted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Layout subsection (container widths, vertical rhythm, card padding)</name>
  <files>design-system/cosmology-group-uba/MASTER.md</files>
  <action>
Add a new top-level `## Layout` section AFTER the Global Rules section (which contains Color / Typography / Type Scale / Spacing / Shadows) and BEFORE the `## Component Specs` section (around line 79 in the current file).

Insert exactly this block:

```
---

## Layout

Codified in v1.2 Phase 13. All measurements encoded as Tailwind classes on components; no CSS custom properties introduced beyond `--text-5xl`.

### Container Widths (SPACE-01)

| Use case | Wrapper width | Applies to |
|----------|---------------|-----------|
| Prose / single-column | `max-w-5xl` (~1024 px) | Contact, PersonDetail, Journal Club, Publications, PeoplePlainSection |
| Grid / multi-column | `max-w-6xl` (~1152 px) | People (grid + page header), Outreach, Research, HomePage sections |
| Inline reading prose | `max-w-3xl` (~768 px) | HomePage intro, any inline prose block inside a wider grid wrapper |

Rule of thumb: if the page is mostly text read top-to-bottom, use `max-w-5xl`. If the page is a grid of cards or imagery, use `max-w-6xl`. HomePage uses `max-w-6xl` outer with `max-w-3xl` inner for prose sections.

### Vertical Rhythm (SPACE-02)

| Scope | Padding | Applies to |
|-------|---------|-----------|
| Page wrapper | `py-16` | Every top-level page `<section>` / `<header>` / `<article>` |
| Sub-section | `py-12` | `PeopleSection`, `PeoplePlainSection`, other in-page sections with their own vertical scope |
| Hero | `py-20` (reserved) | Not applied as a wrapper in v1.2; hero rhythm carried by `HeroCarousel` internal `p-8 md:p-12` + image height. Retained in convention for future full-bleed variants. |

### Card Padding Tiers (SPACE-03)

Two tiers, flat across breakpoints (no responsive step):

| Tier | Padding | Semantic use | Exemplars |
|------|---------|--------------|-----------|
| Dense | `p-4` (or `py-5` for row-based) | "Read a lot of these at once" — grid tiles, list rows | `PersonCard`, `SessionRow` |
| Spacious | `p-6` | "Feature this one" — editorial or hero-card context | `ResearchCard`, `OutreachCard` |

No named utility wrapper (e.g., `.card-dense`, `.card-spacious`) — components carry Tailwind classes directly. Revisit this decision if cards proliferate beyond the four exemplars.

```

Preserve every existing `## Component Specs` content below.
  </action>
  <verify>
```bash
grep -n "^## Layout" design-system/cosmology-group-uba/MASTER.md
# Expected: one match between Global Rules and Component Specs.

grep -n "SPACE-01\|SPACE-02\|SPACE-03" design-system/cosmology-group-uba/MASTER.md
# Expected: all three requirement IDs referenced in the Layout section.

grep -n "max-w-5xl\|max-w-6xl\|max-w-3xl" design-system/cosmology-group-uba/MASTER.md
# Expected: all three widths documented in the container width table.
```

Render MASTER.md in a preview (or just eyeball the markdown): section ordering is Global Rules → Layout → Component Specs.
  </verify>
  <done>
`## Layout` section exists with three subsections (Container Widths, Vertical Rhythm, Card Padding Tiers). Requirement IDs SPACE-01/02/03 cross-referenced. Component Specs section untouched.
  </done>
</task>

</tasks>

<verification>
Final phase-04 checks:
- `grep -n "--text-5xl" design-system/cosmology-group-uba/MASTER.md` → match in Type Scale table.
- `grep -n "^## Layout" design-system/cosmology-group-uba/MASTER.md` → exactly one match.
- `grep -n "^### Line-Height Convention" design-system/cosmology-group-uba/MASTER.md` → one match under Typography.
- Manual read: doc is internally consistent; no orphan references to `2rem` / `32px` for H1.
</verification>

<success_criteria>
- Type Scale table shows `--text-4xl` at 2.25rem/36px and `--text-5xl` at 2.5rem/40px.
- v1.0 scale superseded note present.
- `## Layout` section documents widths, rhythm, and card padding tiers with SPACE-01/02/03 cross-refs.
- `### Line-Height Convention` subsection captures the two-tier rule.
- Commit: `docs(13): document v1.2 type scale, layout widths, rhythm, and card padding`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-tokens-layout-rhythm/13-04-SUMMARY.md` with:
- Sections added/modified in MASTER.md (line ranges)
- Cross-reference map (requirement ID → MASTER.md section)
- Any deviations from the exact block above (e.g., table formatting tweaks)
</output>
