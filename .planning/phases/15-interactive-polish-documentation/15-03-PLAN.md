---
phase: 15-interactive-polish-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/publications/SourceFilter.tsx
  - src/components/publications/PublicationEntry.tsx
autonomous: true

must_haves:
  truths:
    - "SourceFilter pills have ≥36px visual height via px-3.5 py-1.5"
    - "SourceFilter pill toggle (active ↔ inactive) crossfades color via transition-colors duration-150"
    - "All SourceFilter focus rings use ring-accent-ring + ring-offset-2 ring-offset-surface"
    - "All PublicationEntry focus rings use ring-accent-ring + ring-offset-2 ring-offset-surface"
    - "PublicationEntry inline source pills (px-2 py-0.5) are NOT padding-bumped (WCAG inline exception per RESEARCH open-question #3)"
  artifacts:
    - path: "src/components/publications/SourceFilter.tsx"
      provides: "Filter pills at px-3.5 py-1.5 + ring offset + tone transition"
      contains: "px-3.5 py-1.5"
    - path: "src/components/publications/PublicationEntry.tsx"
      provides: "Three focus-ring sites with ring-offset-2 ring-offset-surface added"
      contains: "ring-offset-surface"
  key_links:
    - from: "SourceFilter active vs inactive branches"
      to: "shared transition-colors duration-150"
      via: "hoisted base class string OR repeated on both branches"
      pattern: "transition-colors duration-150"
    - from: "PublicationEntry source pill (line 64) + arXiv link (line 87) + DOI link (line 97)"
      to: "ring-accent-ring + ring-offset-2 ring-offset-surface"
      via: "focus-visible:* utility additions"
      pattern: "ring-offset-surface"
---

<objective>
Publications-component micro-fixes: BTN-04 (SourceFilter `px-3 py-1` → `px-3.5 py-1.5`), MICRO-02 (add explicit `transition-colors duration-150` to both pill branches), and `ring-offset-2 ring-offset-surface` added to ALL 5 focus-ring sites in this slice — SourceFilter:43, SourceFilter:44, PublicationEntry:64, PublicationEntry:87, PublicationEntry:97.

Per RESEARCH.md open-question #3 (planner decision): PublicationEntry inline source pill (px-2 py-0.5, line 60-67) is NOT padding-bumped. It's an inline metadata chip in citation context (WCAG 2.5.5 inline-text exception applies). Documented in 15-06 BTN-06 as deliberately exempt.

Purpose: Closes BTN-04 + MICRO-02 plus adds the `ring-offset-2 ring-offset-surface` pass for all 5 publication-component ring sites. Refactor opportunity (per RESEARCH.md) — hoist common SourceFilter classes to a base const to prevent active/inactive drift.

Output: Two modified files; both pill branches share a base class const for the new transition + sizing + focus-ring pattern.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-interactive-polish-documentation/15-CONTEXT.md
@.planning/phases/15-interactive-polish-documentation/15-RESEARCH.md
@src/components/publications/SourceFilter.tsx
@src/components/publications/PublicationEntry.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SourceFilter — bump padding, add transition, hoist common base, unify focus ring</name>
  <files>src/components/publications/SourceFilter.tsx</files>
  <action>
At SourceFilter.tsx:31-49 (the `<button>` block inside `options.map`), restructure as follows:

  1. Add a `const base` ABOVE the `return` statement inside the component (or above the map callback):
     ```ts
     const base = 'rounded-full px-3.5 py-1.5 text-sm font-medium transition-colors duration-150 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface';
     ```

  2. Replace both branches' inline className strings with concatenations of `base` + the branch-specific tone:
     ```tsx
     <button
       key={opt.key}
       type="button"
       aria-pressed={active}
       onClick={() => { ... }}
       className={
         active
           ? `${base} bg-accent text-white`
           : `${base} bg-surface-alt text-ink-muted hover:text-ink`
       }
     >
       {opt.label}
     </button>
     ```

  Why each token:
  - `px-3.5 py-1.5`: BTN-04 spec (~36px visual height), matches REQUIREMENTS.md and CONTEXT.md exactly.
  - `transition-colors duration-150`: MICRO-02 — the tone change between active/inactive crossfades smoothly. Was missing entirely on both branches per RESEARCH.md.
  - `ring-accent-ring + ring-offset-2 ring-offset-surface`: BTN-02 unification + light-bg offset (CONTEXT.md). The publications page is on the light cream surface.
  - Hoisting to `base`: prevents drift between active/inactive branches when future contributors edit one but not the other (RESEARCH.md "Refactor opportunity for planner").

  Pre-existing `aria-pressed`, `key`, `onClick` props all stay unchanged.
  </action>
  <verify>
1. `grep -n "px-3.5 py-1.5" src/components/publications/SourceFilter.tsx` returns the base const line.
2. `grep -n "transition-colors duration-150" src/components/publications/SourceFilter.tsx` returns the base const line.
3. `grep -nE "ring-accent-ring.*ring-offset-surface" src/components/publications/SourceFilter.tsx` returns the base const line.
4. `grep -n "px-3 py-1[^.]" src/components/publications/SourceFilter.tsx` returns ZERO matches (old padding fully replaced).
5. `pnpm typecheck` passes.
6. `pnpm test` passes (verify any SourceFilter snapshot tests still match — adjust if class strings now differ).
  </verify>
  <done>
SourceFilter has a `base` const containing `rounded-full px-3.5 py-1.5 text-sm font-medium transition-colors duration-150 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface`. Both active and inactive branches use this base + branch-specific bg/text classes. Tone toggle now visibly crossfades over 150ms.
  </done>
</task>

<task type="auto">
  <name>Task 2: PublicationEntry — add ring-offset-surface to 3 focus-ring sites</name>
  <files>src/components/publications/PublicationEntry.tsx</files>
  <action>
Three minimal edits — add `focus-visible:ring-offset-2 focus-visible:ring-offset-surface` to each existing focus-ring className. NO size or padding changes.

  1. Line 64 (source pill): change `'focus-visible:ring-2 focus-visible:ring-accent-ring'` → `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface'`.
  2. Line 87 (arXiv link): change `'focus-visible:ring-2 focus-visible:ring-accent-ring rounded'` → `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'`.
  3. Line 97 (DOI link): change `'focus-visible:ring-2 focus-visible:ring-accent-ring rounded'` → `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'`.

Why no padding bump on line 60-67 source pill (WCAG inline exception per RESEARCH open-question #3): the pill is an inline-flex chip in citation metadata, ~5 per publication entry, clear inline-text context. Treating it as inline-like preserves citation visual rhythm. The exemption is documented in 15-06 (BTN-06).

NO motion changes anywhere in PublicationEntry (per CONTEXT.md "no motion beyond spec").
  </action>
  <verify>
1. `grep -cE "ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface" src/components/publications/PublicationEntry.tsx` returns 3.
2. `grep -cE "focus-visible:ring-accent-ring[^ ]*$" src/components/publications/PublicationEntry.tsx` (looking for ring-accent-ring at end of attribute, no offset) returns ZERO — every site got the offset.
3. `grep -nE "px-2 py-0.5" src/components/publications/PublicationEntry.tsx` STILL returns the source pill line (NOT bumped — WCAG inline exception applied per planner decision).
4. `pnpm typecheck` passes.
  </verify>
  <done>
All 3 focus-ring sites in PublicationEntry.tsx (source pill, arXiv link, DOI link) include `ring-offset-2 ring-offset-surface`. Source pill padding (`px-2 py-0.5`) is unchanged — WCAG inline-text exception applied per documented planner decision.
  </done>
</task>

</tasks>

<verification>
- Both files compile (`pnpm typecheck`).
- All 5 ring sites in this slice (SourceFilter:43-44 → 1 base const, PublicationEntry:64+87+97) include `ring-offset-surface`.
- `pnpm build` passes.
- `pnpm test` passes (adjust SourceFilter snapshot expectations if applicable).
- Drift-gate readiness: zero `focus-visible:ring-` sites in these 2 files use anything except `ring-accent-ring`.
</verification>

<success_criteria>
1. `grep -nE "focus-visible:ring-(?!2|accent-ring|offset-)" src/components/publications/SourceFilter.tsx src/components/publications/PublicationEntry.tsx` returns ZERO hits.
2. SourceFilter has a single hoisted `base` const including all the BTN-04 + MICRO-02 + BTN-02 utilities.
3. PublicationEntry has 3 focus rings with offset added; source pill padding unchanged.
4. `pnpm build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/15-interactive-polish-documentation/15-03-SUMMARY.md` documenting:
- Final SourceFilter base const string
- Confirmation that PublicationEntry source pill was NOT padding-bumped (WCAG exception per planner decision)
- Atomic commits made: `feat(15-03): SourceFilter pill sizing + transition + ring offset`, `feat(15-03): PublicationEntry add ring offset to 3 focus sites`
</output>
