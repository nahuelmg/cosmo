---
phase: 15-interactive-polish-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/NavLink.tsx
  - src/components/layout/LocaleToggle.tsx
  - src/components/layout/MobileNav.tsx
autonomous: true

must_haves:
  truths:
    - "Desktop NavLink instances have ≥44px tall hit row"
    - "LocaleToggle has ≥44px tall hit row matching NavLink rhythm"
    - "MobileNav trigger and close buttons have ≥44×44 hit area"
    - "NavLink active-state color change uses explicit `transition-colors duration-150`"
    - "All header focus rings render with `ring-accent-ring + ring-offset-2 ring-offset-surface`"
    - "SiteHeader visible row height (h-24, 96px) is unchanged"
  artifacts:
    - path: "src/components/layout/NavLink.tsx"
      provides: "NavLink with py-1.5 hit padding + explicit duration-150 transition"
      contains: "py-1.5"
    - path: "src/components/layout/LocaleToggle.tsx"
      provides: "LocaleToggle with py-1.5 hit padding + ring offset"
      contains: "py-1.5"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "MobileNav trigger/close at w-11 h-11"
      contains: "w-11 h-11"
  key_links:
    - from: "NavLink active-state"
      to: "transition-colors duration-150"
      via: "base const in NavLink.tsx"
      pattern: "transition-colors duration-150"
    - from: "MobileNav trigger button"
      to: "44×44 hit zone"
      via: "w-11 h-11 utility"
      pattern: "w-11 h-11"
---

<objective>
Header micro-fixes: BTN-05 (NavLink + LocaleToggle 44px hit rows via `py-1.5`), MICRO-01 (NavLink active-state explicit `transition-colors duration-150`), MobileNav trigger/close bump from 40×40 → 44×44 (RESEARCH open-question #2: include in BTN-01 sweep), and `ring-offset-2 ring-offset-surface` added to all 4 focus-ring sites in this slice (LocaleToggle:104, MobileNav:53, MobileNav:102 — and NavLink does NOT have its own ring; it inherits from parent).

Purpose: Closes BTN-05 and MICRO-01, plus the MobileNav tap-target follow-up that RESEARCH.md flagged. Visible header row height (`h-24` 96px) stays unchanged — only hit zones grow.

Output: Three modified files with consistent 44px hit zones across the header navigation cluster, explicit transition timing on NavLink, and unified focus-ring offsets on light backgrounds.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-interactive-polish-documentation/15-CONTEXT.md
@.planning/phases/15-interactive-polish-documentation/15-RESEARCH.md
@src/components/layout/NavLink.tsx
@src/components/layout/LocaleToggle.tsx
@src/components/layout/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavLink — add py-1.5 hit padding + explicit duration-150 transition</name>
  <files>src/components/layout/NavLink.tsx</files>
  <action>
At NavLink.tsx:32 (the `base` constant): change `const base = 'transition-colors';` to `const base = 'transition-colors duration-150 py-1.5';`.

  - Why `duration-150`: MICRO-01 names exactly this duration; CONTEXT.md "Default duration is `duration-150` with ease-out". Tailwind's `duration-150` defaults to ease-in-out; the `transition-colors` utility uses cubic-bezier(0.4, 0, 0.2, 1). No additional `ease-out` needed (Tailwind default is acceptable per CONTEXT.md "ease-out" intent on simple color crossfades).
  - Why `py-1.5` (6px each side): combined with the inherited `text-sm` line-height (~20px) gives ~32px text-row, then SiteHeader's `h-24` (96px) flex-center provides the rest of the 44px hit row. CONTEXT.md "BTN-05 NavLink + LocaleToggle: apply `py-1.5` so the combined hit area is 44 px tall, but the visible header chrome stays its current height. Phase 13's nav rhythm is preserved; only the hit zone grows."
  - This adds `py-1.5` to BOTH desktop and mobile NavLink usages. Mobile NavLink is wrapped at `MobileNav.tsx:128` with `text-sm py-3 px-2 rounded` className prop — adding `py-1.5` to base means mobile gets py-1.5 + py-3 stacked. Tailwind class merging: `py-3` (later in className string) wins via specificity within `<Link>` — verify the existing precedence by inspecting className concatenation order in NavLink.tsx:36-39:
    ```
    const combined = [
      base,
      isActive ? `${active} ${activeClassName}` : inactive,
      className,  // <-- mobile passes "text-sm py-3 px-2 rounded" here, comes LAST
    ].filter(Boolean).join(' ');
    ```
    Since `className` (mobile's `py-3`) is concatenated AFTER `base` (now `py-1.5`), and Tailwind merges by source order via the JIT class-name string, the LATER class wins in CSS specificity. `py-3` overrides `py-1.5` in the cascade for mobile. SAFE — mobile stays at py-3 (~50px hit) because that string appears later.
  - If concerned about this Tailwind merge behavior, RESEARCH.md confirms mobile NavLink already exceeds 44 (`MobileNav.tsx:128` "py-3 = 24px vertical → ~50px hit. No change required for mobile — already compliant"). Either way the final mobile hit row is ≥44px.

Leave the rest of the file unchanged (the `<Link>` element, `inactive`, `active` consts, etc.).
  </action>
  <verify>
1. `grep -n "transition-colors duration-150 py-1.5" src/components/layout/NavLink.tsx` returns the base const line.
2. `pnpm typecheck` passes.
3. Manually inspect that MobileNav still renders mobile NavLinks with `py-3` (i.e., 50px hit row) by searching for `MobileNav.tsx:128` className usage — if `py-3` appears AFTER `py-1.5` in the resolved className string, mobile is fine.
  </verify>
  <done>
NavLink base const includes `transition-colors duration-150 py-1.5`. Desktop NavLink hit row reaches 44px via py-1.5 inside the 96px header. Mobile NavLink still renders at py-3 (drawer's className overrides base via Tailwind cascade source order).
  </done>
</task>

<task type="auto">
  <name>Task 2: LocaleToggle — bump to py-1.5 + add ring offset</name>
  <files>src/components/layout/LocaleToggle.tsx</files>
  <action>
At LocaleToggle.tsx:85-113 (the `<button>` className array), make TWO edits:

  1. Change `'px-2 py-1'` to `'px-2 py-1.5'`.
     - Why: BTN-05 pairs LocaleToggle's hit row with NavLink's at 44px. py-1.5 (12px) on text-sm gives ~32px text-row; combined with the 96px header h-24 flex-center, hit row is ≥44px.
  2. Change `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:rounded'` to `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface focus-visible:rounded'`.
     - Why ring-offset-surface: CONTEXT.md "ring-offset-surface on light backgrounds" — the header surface is light cream (--color-surface). NO new --ring-offset theme token.

Pre-existing `transition-[color,transform] duration-75` and `active:scale-95` stay (NOT in MICRO-01..03 scope per CONTEXT.md "no motion beyond spec").
  </action>
  <verify>
1. `grep -n "px-2 py-1.5" src/components/layout/LocaleToggle.tsx` returns the className line.
2. `grep -nE "ring-accent-ring.*ring-offset-surface" src/components/layout/LocaleToggle.tsx` returns the focus-ring line.
3. `grep -n "px-2 py-1[^.]" src/components/layout/LocaleToggle.tsx` returns ZERO matches (old py-1 should be replaced).
4. `pnpm typecheck` passes.
  </verify>
  <done>
LocaleToggle uses `px-2 py-1.5` and focus-visible ring uses `ring-accent-ring + ring-offset-2 ring-offset-surface`.
  </done>
</task>

<task type="auto">
  <name>Task 3: MobileNav trigger + close — bump to 44×44 + add ring offset</name>
  <files>src/components/layout/MobileNav.tsx</files>
  <action>
Two button bumps and two focus-ring offset additions.

**Trigger button (lines 43-54):**
  1. Change `'w-10 h-10'` to `'w-11 h-11'`.
     - Why: RESEARCH.md open-question #2 — MobileNav trigger is currently 40×40, 4px short of 44. CONTEXT.md "bump to `w-11 h-11` is acceptable" applies.
  2. Change `'focus-visible:ring-2 focus-visible:ring-accent-ring'` to `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface'`.

**Close button (lines 94-103):**
  1. Change `'w-10 h-10'` to `'w-11 h-11'`.
  2. Change `'focus-visible:ring-2 focus-visible:ring-accent-ring'` to `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface'`.

Pre-existing `transition-[color,transform] duration-75` and `active:scale-95` on both buttons stay (NOT in MICRO scope).

The drawer panel itself is on a light surface (cream) — `ring-offset-surface` is correct for both buttons.
  </action>
  <verify>
1. `grep -nE "w-11 h-11" src/components/layout/MobileNav.tsx` returns BOTH the trigger and close button lines (2 matches).
2. `grep -nE "w-10 h-10" src/components/layout/MobileNav.tsx` returns ZERO matches (old size replaced on both).
3. `grep -cE "ring-accent-ring.*ring-offset-surface" src/components/layout/MobileNav.tsx` returns 2 (both focus rings updated).
4. `pnpm typecheck` passes.
  </verify>
  <done>
MobileNav trigger and close are both `w-11 h-11` (44×44); both focus rings use `ring-accent-ring + ring-offset-2 ring-offset-surface`; mobile NavLink links inside the drawer are unchanged (already compliant per RESEARCH.md).
  </done>
</task>

</tasks>

<verification>
- All 3 files compile (`pnpm typecheck`).
- All edits land on the exact lines RESEARCH.md identified.
- Header h-24 (96px) is preserved (not touched by these edits).
- `pnpm build` passes.
- Drift gate readiness: zero `focus-visible:ring-` sites in these 3 files use anything except `ring-accent-ring` (now also with `ring-offset-2 ring-offset-surface`).
</verification>

<success_criteria>
1. `grep -nE "focus-visible:ring-(?!2|accent-ring|offset-)" src/components/layout/NavLink.tsx src/components/layout/LocaleToggle.tsx src/components/layout/MobileNav.tsx` returns ZERO hits.
2. NavLink base const has `transition-colors duration-150 py-1.5`.
3. LocaleToggle uses `px-2 py-1.5`.
4. MobileNav trigger and close are both `w-11 h-11`.
5. All 3 ring sites in this slice (LocaleToggle:104, MobileNav:53, MobileNav:102) include `ring-offset-2 ring-offset-surface`.
6. `pnpm build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/15-interactive-polish-documentation/15-02-SUMMARY.md` documenting:
- Confirm py-1.5 + h-24 = effective 44px hit row (do quick math)
- Confirm Tailwind class-merge precedence kept mobile NavLink at py-3
- Atomic commits made: `feat(15-02): bump NavLink hit row + explicit duration-150`, `feat(15-02): bump LocaleToggle hit row + ring offset`, `feat(15-02): bump MobileNav trigger/close to 44x44 + ring offset`
</output>
