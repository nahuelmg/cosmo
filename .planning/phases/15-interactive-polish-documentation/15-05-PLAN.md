---
phase: 15-interactive-polish-documentation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/PartnerStrip.tsx
  - src/components/contact/ContactDetails.tsx
  - src/components/contact/MapEmbed.tsx
  - src/components/journal-club/SessionRow.tsx
  - src/components/layout/SiteFooter.tsx
  - src/components/layout/SiteHeader.tsx
  - src/components/outreach/OutreachCard.tsx
autonomous: true

must_haves:
  truths:
    - "All 8 remaining focus-ring sites in this slice include ring-offset-2 ring-offset-surface"
    - "All 8 sites still use ring-accent-ring (unchanged)"
    - "No padding/sizing changes to inline-text links (WCAG 2.5.5 inline exception applied per planner decision)"
    - "SkipLink (focus: not focus-visible:) is NOT touched (Phase 3 deliberate pattern)"
  artifacts:
    - path: "src/components/home/PartnerStrip.tsx"
      provides: "Ring offset added to focus-visible site (line 21)"
      contains: "ring-offset-surface"
    - path: "src/components/contact/ContactDetails.tsx"
      provides: "Ring offset added (line 62)"
      contains: "ring-offset-surface"
    - path: "src/components/contact/MapEmbed.tsx"
      provides: "Ring offset added (line 48)"
      contains: "ring-offset-surface"
    - path: "src/components/journal-club/SessionRow.tsx"
      provides: "Ring offset added (line 47)"
      contains: "ring-offset-surface"
    - path: "src/components/layout/SiteFooter.tsx"
      provides: "Ring offset added to 2 sites (lines 86, 102)"
      contains: "ring-offset-surface"
    - path: "src/components/layout/SiteHeader.tsx"
      provides: "Ring offset added to logo Link (line 74)"
      contains: "ring-offset-surface"
    - path: "src/components/outreach/OutreachCard.tsx"
      provides: "Ring offset added to learn-more link (line 48)"
      contains: "ring-offset-surface"
  key_links:
    - from: "Every focus-visible:ring-* site in these 7 files"
      to: "ring-accent-ring + ring-offset-2 ring-offset-surface"
      via: "single utility append per site"
      pattern: "ring-offset-surface"
---

<objective>
Distributed `ring-offset-2 ring-offset-surface` sweep across the remaining 8 `focus-visible:ring-accent-ring` sites that are NOT touched by 15-01..15-04. All 8 sites already use `ring-accent-ring` per RESEARCH.md audit (only HeroCarousel had `ring-surface/70` — handled in 15-01) — this is purely an offset-addition pass.

Per RESEARCH.md open-question #1 (planner decision): inline-text links inside these files (PartnerStrip, ContactDetails social, SessionRow paper-link, SiteFooter EmailLink, OutreachCard learn-more) are NOT padding-bumped — WCAG 2.5.5 inline-text exception applies. Documented in 15-06 BTN-06.

Per RESEARCH.md SkipLink note: `src/components/layout/SkipLink.tsx:40` uses `focus:ring-` (not `focus-visible:ring-`) — Phase 3 deliberate pattern, sr-only chip is keyboard-only by definition. NOT touched in this sweep.

Purpose: Closes the focus-ring-offset half of MICRO-05 across all remaining sites. Single mechanical pass — adds the same 2 utility classes to each site. Together with 15-01..04, this means EVERY `focus-visible:ring-*` site in `src/` will use `ring-accent-ring + ring-offset-2 ring-offset-{surface|black/40}` — preparing the codebase for the drift gate in 15-06.

Output: 7 modified files, 8 ring sites updated.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-interactive-polish-documentation/15-CONTEXT.md
@.planning/phases/15-interactive-polish-documentation/15-RESEARCH.md
@src/components/home/PartnerStrip.tsx
@src/components/contact/ContactDetails.tsx
@src/components/contact/MapEmbed.tsx
@src/components/journal-club/SessionRow.tsx
@src/components/layout/SiteFooter.tsx
@src/components/layout/SiteHeader.tsx
@src/components/outreach/OutreachCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ring-offset-2 ring-offset-surface to all 8 remaining sites</name>
  <files>src/components/home/PartnerStrip.tsx, src/components/contact/ContactDetails.tsx, src/components/contact/MapEmbed.tsx, src/components/journal-club/SessionRow.tsx, src/components/layout/SiteFooter.tsx, src/components/layout/SiteHeader.tsx, src/components/outreach/OutreachCard.tsx</files>
  <action>
For each of the 8 sites listed below, append `focus-visible:ring-offset-2 focus-visible:ring-offset-surface` to the existing `focus-visible:ring-2 focus-visible:ring-accent-ring` className. Order in the className string: place the offset utilities IMMEDIATELY AFTER `focus-visible:ring-accent-ring` (preserves Tailwind utility grouping convention).

Exact site list (from RESEARCH.md table — line numbers may have shifted by ±1 if 15-01..04 land first; if they don't (Wave 1 parallel), original lines are correct):

  1. **`src/components/home/PartnerStrip.tsx:21`** — change `'focus-visible:ring-2 focus-visible:ring-accent-ring'` → `'focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface'`.
     - Pre-existing classes around it (`font-serif text-lg`) — leave alone. Inline link, NO padding bump (WCAG inline exception).

  2. **`src/components/contact/ContactDetails.tsx:62`** — same pattern (existing has `... ring-accent-ring rounded`); change to `'... ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'` (preserve `rounded` at end).

  3. **`src/components/contact/MapEmbed.tsx:48`** — same pattern. Light page background; `ring-offset-surface` correct.

  4. **`src/components/journal-club/SessionRow.tsx:47`** — existing has `'... ring-accent-ring rounded self-start'`; change to `'... ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded self-start'`.

  5. **`src/components/layout/SiteFooter.tsx:86`** (EmailLink) — existing `'... ring-accent-ring rounded'`; change to `'... ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'`.

  6. **`src/components/layout/SiteFooter.tsx:102`** (social link, currently dormant — empty list) — same pattern as #5. Even though dormant, keep parity with the rest of the codebase.

  7. **`src/components/layout/SiteHeader.tsx:74`** (logo Link) — existing `'... ring-accent-ring rounded'`; change to `'... ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'`.

  8. **`src/components/outreach/OutreachCard.tsx:48`** (learn-more link) — existing `'... ring-accent-ring rounded'`; change to `'... ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface rounded'`.

**DO NOT TOUCH:**
- `src/components/layout/SkipLink.tsx:40` — uses `focus:` not `focus-visible:`, Phase 3 deliberate pattern (per RESEARCH.md SkipLink note).
- HeroCarousel.tsx — handled by 15-01 (uses `ring-offset-black/40` per dark/image background convention).
- NavLink.tsx, LocaleToggle.tsx, MobileNav.tsx — handled by 15-02.
- SourceFilter.tsx, PublicationEntry.tsx — handled by 15-03.
- PersonCard.tsx — handled by 15-04.

**NO padding bumps anywhere in this plan.** Per RESEARCH.md open-question #1 (planner decision): inline-text links (OutreachCard learn-more, PartnerStrip, SessionRow paper-link, ContactDetails social, SiteFooter EmailLink) fall under WCAG 2.5.5 AAA inline-text exception — they are deliberately exempt from the 44×44 rule. SiteHeader logo (≥64px hit zone via 64×64 logo) and MapEmbed fallback (full-container fill, 16:9 aspect) already exceed 44.

Use a single Bash/Grep workflow to verify each site sequentially, OR perform 8 small Edit calls — either works. The change is mechanical: append the same 2 utility classes to each site.
  </action>
  <verify>
1. `grep -rcE "focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface" src/components/home/PartnerStrip.tsx src/components/contact/ContactDetails.tsx src/components/contact/MapEmbed.tsx src/components/journal-club/SessionRow.tsx src/components/layout/SiteFooter.tsx src/components/layout/SiteHeader.tsx src/components/outreach/OutreachCard.tsx` returns counts: PartnerStrip=1, ContactDetails=1, MapEmbed=1, SessionRow=1, SiteFooter=2, SiteHeader=1, OutreachCard=1 (total 8).
2. `grep -rE "focus-visible:ring-accent-ring(?!.*ring-offset-)" src/components/home/PartnerStrip.tsx src/components/contact/ContactDetails.tsx src/components/contact/MapEmbed.tsx src/components/journal-club/SessionRow.tsx src/components/layout/SiteFooter.tsx src/components/layout/SiteHeader.tsx src/components/outreach/OutreachCard.tsx -P` (PCRE lookahead) returns ZERO matches — every site got the offset.
3. SkipLink unchanged: `grep -n "focus:ring-2 focus:ring-accent-ring" src/components/layout/SkipLink.tsx` still returns line 40.
4. `pnpm typecheck` passes.
5. `pnpm test` passes.
6. `pnpm build` passes.
  </verify>
  <done>
All 8 `focus-visible:ring-accent-ring` sites in these 7 files include `focus-visible:ring-offset-2 focus-visible:ring-offset-surface`. SkipLink is untouched. No padding/sizing changes to any inline-text link.
  </done>
</task>

</tasks>

<verification>
- All 7 files compile (`pnpm typecheck`).
- `pnpm build` passes.
- Combined with 15-01..04, EVERY `focus-visible:ring-*` site in `src/` (19 sites total) uses `ring-accent-ring + ring-offset-2 ring-offset-{surface|black/40}`.
- SkipLink (`focus:` not `focus-visible:`) preserved.
</verification>

<success_criteria>
1. `grep -rE "focus-visible:ring-(?!2|accent-ring|offset-)" src/` returns ZERO hits across the entire `src/` tree (sweep complete across all 5 wave-1 plans).
2. `grep -rE "focus-visible:ring-accent-ring(?!.*ring-offset-)" src/ -P` returns ZERO hits across all 19 sites.
3. SkipLink.tsx still uses `focus:ring-2 focus:ring-accent-ring` (intentional Phase 3 pattern).
4. `pnpm build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/15-interactive-polish-documentation/15-05-SUMMARY.md` documenting:
- Confirm 8 sites updated (per file count)
- Confirm SkipLink intentionally untouched
- Confirm zero inline-text links got padding bumps (WCAG inline exception applied per planner decision)
- Atomic commit: `feat(15-05): add ring-offset-surface across 8 remaining focus-ring sites`
</output>
