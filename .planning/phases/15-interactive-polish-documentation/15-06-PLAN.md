---
phase: 15-interactive-polish-documentation
plan: 06
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05"]
files_modified:
  - package.json
  - .github/workflows/lint-rings.yml
  - design-system/cosmology-group-uba/MASTER.md
  - design-system/cosmology-group-uba/OVERRIDES.md
autonomous: false

must_haves:
  truths:
    - "`pnpm axe` script exists in package.json and invokes axe-core CLI against all 8 Spanish pages"
    - "`pnpm axe` reports zero violations against a running dev server (verified during this plan)"
    - "GitHub Actions workflow `.github/workflows/lint-rings.yml` runs on push/PR and fails on disallowed `focus-visible:ring-*` patterns"
    - "MASTER.md `## Component Specs` section contains Tailwind recipes (not raw CSS) for: button, pill, dot, nav link, locale toggle, carousel pause/play"
    - "MASTER.md Component Specs documents the focus-ring pattern (ring-accent-ring + offset) and the tap-target rule (≥44×44 + WCAG inline exception)"
    - "OVERRIDES.md has a new `## v1.2 Overrides` section in table format with all v1.2 deltas"
    - "Existing v1.0 numbered-list overrides in OVERRIDES.md are preserved"
  artifacts:
    - path: "package.json"
      provides: "pnpm axe script"
      contains: "axe-core/cli"
    - path: ".github/workflows/lint-rings.yml"
      provides: "CI drift gate for focus-ring uniformity"
      contains: "focus-visible:ring-"
    - path: "design-system/cosmology-group-uba/MASTER.md"
      provides: "v1.2 Component Specs in Tailwind recipes"
      contains: "Tailwind"
    - path: "design-system/cosmology-group-uba/OVERRIDES.md"
      provides: "v1.2 Overrides table"
      contains: "v1.2 Overrides"
  key_links:
    - from: "package.json scripts"
      to: "8 Spanish page URLs"
      via: "npx -y @axe-core/cli@4 invocation"
      pattern: "@axe-core/cli"
    - from: ".github/workflows/lint-rings.yml"
      to: "src/ tree"
      via: "grep -P regex on push/PR"
      pattern: "focus-visible:ring-"
    - from: "MASTER.md Component Specs"
      to: "actual Tailwind utility class strings used in 15-01..05"
      via: "copy-pasteable recipe blocks"
      pattern: "ring-accent-ring"
---

<objective>
Final phase plan: docs + verification + drift gate. Three concerns rolled into one Wave-2 plan because they all run AGAINST the finished v1.2 codebase landed by 15-01..05:

  1. **Add `pnpm axe` script + run it (MICRO-04)** — RESEARCH.md confirmed the script does NOT exist; mirrors Phase 6 invocation pattern (`npx -y @axe-core/cli@4 <8 hardcoded URLs> --load-delay 1500`). Hardcoded URL list per RESEARCH open-question #5 (planner decision). Run against `pnpm dev` to confirm zero violations.
  2. **Add CI drift gate (MICRO-05 enforcement + BTN-02 prevention)** — `.github/workflows/lint-rings.yml` per RESEARCH open-question #6 (planner decision). Uses `grep -P` PCRE lookahead per RESEARCH.md pattern. Replaces ad-hoc enforcement with automatic check on push/PR.
  3. **Update MASTER.md + OVERRIDES.md (DOC-01, DOC-02, BTN-06)** — replace MASTER.md `## Component Specs` raw-CSS blocks with Tailwind recipes for the 6 v1.2 components; append `## v1.2 Overrides` table to OVERRIDES.md per CONTEXT.md "append" decision (RESEARCH open-question on file structure resolved: Option 1).

Plan is `autonomous: false` — includes a final `checkpoint:human-verify` for the visual sweep at 375 / 1024 / 1440 px (MICRO-05's manual-audit half) per CONTEXT.md "manual viewport audit at 375 / 1024 / 1440 px — tab through Hero controls, Publications source-filter pills, People cards. Capture pass/fail per element. Same audit shape Phase 14-03 used. Does NOT rely on `pnpm axe` for ring contrast."

Purpose: Closes BTN-06 + DOC-01 + DOC-02 + MICRO-04 + MICRO-05 (manual half). Locks in the drift gate so future contributors can't reintroduce stray ring colors.

Output: 4 modified/created files + 1 dev-server axe run + 1 human visual sweep.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-interactive-polish-documentation/15-CONTEXT.md
@.planning/phases/15-interactive-polish-documentation/15-RESEARCH.md
@.planning/phases/15-interactive-polish-documentation/15-01-SUMMARY.md
@.planning/phases/15-interactive-polish-documentation/15-02-SUMMARY.md
@.planning/phases/15-interactive-polish-documentation/15-03-SUMMARY.md
@.planning/phases/15-interactive-polish-documentation/15-04-SUMMARY.md
@.planning/phases/15-interactive-polish-documentation/15-05-SUMMARY.md
@package.json
@design-system/cosmology-group-uba/MASTER.md
@design-system/cosmology-group-uba/OVERRIDES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `pnpm axe` script + CI drift-gate workflow + run axe to verify zero violations</name>
  <files>package.json, .github/workflows/lint-rings.yml</files>
  <action>
**Sub-step 1A: Add `axe` script to package.json.**

In the `scripts` block, add a new `axe` entry (alphabetical or grouped at the bottom):

```json
"axe": "npx -y @axe-core/cli@4 http://localhost:3000/es http://localhost:3000/es/personas http://localhost:3000/es/personas/esteban-calzetta http://localhost:3000/es/investigacion http://localhost:3000/es/publicaciones http://localhost:3000/es/contacto http://localhost:3000/es/divulgacion http://localhost:3000/es/journal-club --load-delay 1500"
```

Why hardcoded URLs (per RESEARCH open-question #5 + CONTEXT.md): mirrors Phase 6 invocation exactly, no devDep churn (axe-core/cli is `npx -y` on demand), only 8 URLs, slug stability has been zero-change in 14 phases. Cross-reference `src/i18n/routing.ts` — Spanish slugs: `personas`, `investigacion`, `publicaciones`, `contacto`, `divulgacion`, `journal-club` (NB: stays `/journal-club`, NOT `/club-de-revista`).

**Sub-step 1B: Create `.github/workflows/lint-rings.yml`.**

```yaml
name: Lint Focus Rings

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-rings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check focus-visible:ring-* uses ring-accent-ring exclusively
        run: |
          # Permitted patterns: ring-2 (size), ring-accent-ring (color), ring-offset-* (offset utilities).
          # Anything else (e.g. ring-surface/70, ring-blue-500) fails the build.
          violations=$(grep -rEnP 'focus-visible:ring-(?!2($|\s|"|'\''|/|\\)|accent-ring|offset-)' src/ || true)
          if [ -n "$violations" ]; then
            echo "::error::Found focus-visible:ring-* uses outside ring-accent-ring + ring-offset-*:"
            echo "$violations"
            exit 1
          fi
          echo "OK: all focus-visible:ring-* sites use ring-accent-ring (with optional offset)"
```

Why this regex (per RESEARCH open-question #4): the lookahead negates `ring-2` (size, with anchor on word-boundary chars to avoid false positive on `ring-200` or similar), `ring-accent-ring` (allowed color), and `ring-offset-` (allowed offset utility prefix). Excludes SkipLink (`focus:` not `focus-visible:`) automatically. Tested intent: should pass post-15-01..05 (zero violations) and would have failed pre-fix on HeroCarousel `ring-surface/70` (2 hits). RESEARCH.md "Suggest the planner test the regex against the current state of HeroCarousel BEFORE the fix lands (should produce 2 hits) and AFTER (should produce 0)".

Use `grep -rEnP` (GNU grep with PCRE) since GitHub Actions runners (`ubuntu-latest`) ship GNU grep with PCRE2 enabled.

**Sub-step 1C: Run axe locally to verify zero violations.**

```bash
# Start dev server in background
pnpm dev &
DEV_PID=$!
sleep 10  # allow Next.js + Turbopack startup

# Run axe (per Phase 6 precedent — may need CHROME_TEST_PATH workaround)
# RESEARCH.md note: "06-01-SUMMARY.md:224-226 documents the CHROME_TEST_PATH=~/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome workaround used after running npx playwright install chromium"
pnpm axe || (echo "FAILED — try: CHROME_TEST_PATH=~/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome pnpm axe"; npx playwright install chromium && pnpm axe)

# Capture output, kill dev server
kill $DEV_PID
```

Expected outcome: zero violations across all 8 pages (Phase 6 baseline preserved per `06-03-SUMMARY.md`). If violations appear, they MUST be triaged in this plan before proceeding to Task 2 — they likely indicate regressions from 15-01..05.

If the local Chrome binary is missing, `npx playwright install chromium` and rerun with `CHROME_TEST_PATH=~/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome pnpm axe`. Document the working invocation in the SUMMARY.
  </action>
  <verify>
1. `cat package.json | grep '"axe"'` returns the new script line.
2. `cat .github/workflows/lint-rings.yml` returns the file content (workflow created).
3. Dry-run the lint-rings regex locally to confirm zero current violations:
   `grep -rEnP 'focus-visible:ring-(?!2($|\s|"|'\''|/|\\)|accent-ring|offset-)' src/ || echo "PASS: no violations"`
   Expect "PASS: no violations" output.
4. `pnpm axe` against running dev server reports `0 violations` across all 8 URLs.
  </verify>
  <done>
`pnpm axe` script in package.json + `.github/workflows/lint-rings.yml` workflow file created. Axe scan passes with 0 violations against all 8 Spanish pages. Drift-gate regex confirms zero `focus-visible:ring-*` sites outside the allowed pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MASTER.md `## Component Specs` to Tailwind recipes + append OVERRIDES.md v1.2 table</name>
  <files>design-system/cosmology-group-uba/MASTER.md, design-system/cosmology-group-uba/OVERRIDES.md</files>
  <action>
**Sub-step 2A: REPLACE `## Component Specs` section in MASTER.md (lines 130-217 per RESEARCH.md TOC).**

Per RESEARCH.md "Critical observation for BTN-06: The current `## Component Specs` section uses raw CSS class blocks (`.btn-primary { ... }`), NOT Tailwind class strings... codebase has zero `.btn-primary` classes (Tailwind utility-only)... **Recommendation:** replace."

Per CONTEXT.md "MASTER.md reads as current source-of-truth, not history of versions" — REPLACE the existing CSS blocks entirely. Do NOT preserve `.btn-primary` / `.btn-secondary` / `.card` / `.input` / `.modal` blocks — they have no codebase analogues.

New `## Component Specs` content (replacing lines 130-217). Use Tailwind recipes for the 6 components named in CONTEXT.md/BTN-06: button, pill, dot, nav link, locale toggle, carousel pause/play. Plus a short focus-ring + tap-target rules subsection at the top.

```markdown
## Component Specs

All components use Tailwind utility classes inline (no `.btn-primary` etc. abstractions). The recipes below are copy-pasteable starting points for new components.

### Universal Rules

**Focus ring pattern (BTN-02, MICRO-05):**
- Color: `ring-accent-ring` everywhere (no exceptions in `focus-visible:` — SkipLink uses `focus:` deliberately).
- Offset: `ring-offset-2` always; offset color per surface:
  - Light backgrounds (cream `--color-surface`): `ring-offset-surface`
  - Dark/image backgrounds (Hero JWST starfield): `ring-offset-black/40`
- Full pattern: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-{surface|black/40}`

**Tap-target rule (BTN-01..05):**
- Every interactive button or non-inline link has an effective ≥44×44 px hit area.
- Visible chrome may be smaller; padding-inside compensates (e.g., 10×10 carousel dots inside 44×44 `p-[17px]` buttons).
- **Inline-text links exempted** per WCAG 2.5.5 AAA inline exception: links inside paragraph text (PartnerStrip, OutreachCard learn-more, SiteFooter EmailLink, ContactDetails social, SessionRow paper-link, PublicationEntry arXiv/DOI/source-pill metadata) deliberately do NOT receive padding bumps.

**Motion (MICRO-01..03):** strictly limited to 3 surfaces — NavLink active-state, SourceFilter pill toggle, PersonCard photo zoom. All transitions wrap in `motion-safe:` to respect `prefers-reduced-motion`. Default duration `duration-150`; PersonCard zoom uses `duration-200` (per-spec exception).

### Buttons

**Carousel pause/play (HeroCarousel):**
```html
<button class="w-11 h-11 flex items-center justify-center rounded
  text-surface/90 hover:text-surface
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-black/40
  transition-colors">
  <Icon class="w-3.5 h-3.5" aria-hidden />
</button>
```
- Visible chrome: 44×44 (was 24×24 in v1.0).
- Hit area: 44×44.
- Focus ring: dark/image-bg pattern.

**MobileNav trigger / close:**
```html
<button class="w-11 h-11 ... focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface">
  <Icon ... />
</button>
```
- Visible chrome + hit area: 44×44 (was 40×40 in v1.0).

### Pills

**SourceFilter pill (publications page filter):**
```html
<button class="rounded-full px-3.5 py-1.5 text-sm font-medium
  transition-colors duration-150
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface
  {active ? 'bg-accent text-white' : 'bg-surface-alt text-ink-muted hover:text-ink'}">
  {label}
</button>
```
- Visible height: ≥36 px (px-3.5 py-1.5).
- Hit area: ≥36 × content-width (within 44 px tolerance for short labels).
- Tone toggle: `transition-colors duration-150` (MICRO-02).

### Carousel Dots

**HeroCarousel dot (padding-inside pattern):**
```html
<button class="p-[17px] rounded-full flex items-center justify-center
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-black/40">
  <span aria-hidden class="block w-2.5 h-2.5 rounded-full
    active:scale-95 transition-[background-color,transform] duration-75
    {active ? 'bg-surface' : 'bg-surface/50'}" />
</button>
```
- Visible chrome: 10×10 dot (preserved from v1.0).
- Hit area: 44×44 via `p-[17px]` (17 + 10 + 17 = 44).
- Wrapper uses `gap-0` so adjacent dots tile edge-to-edge with no overlap.

### Nav Link

**Desktop NavLink (header):**
```html
<a class="transition-colors duration-150 py-1.5
  text-ink-muted hover:text-ink
  {active && 'text-accent font-semibold'}">
  {children}
</a>
```
- Hit row: 44 px (py-1.5 inside h-24 header flex-center).
- Visible chrome: text-sm + py-1.5 (~32 px text row).
- Active-state crossfade: `transition-colors duration-150` (MICRO-01, explicit per spec).

### Locale Toggle

```html
<button class="text-sm font-semibold tracking-wide
  text-ink-muted hover:text-ink
  px-2 py-1.5
  transition-[color,transform] duration-75 active:scale-95
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-surface focus-visible:rounded">
  {ES|EN}
</button>
```
- Hit row: 44 px (py-1.5 inside h-24 header flex-center, paired with NavLink).
- Pre-existing `transition-[color,transform] duration-75` preserved (NOT in MICRO-01..03 motion scope).

### PersonCard (image zoom on hover)

The Image inside the card adds:
```html
<Image class="object-cover
  motion-safe:transition-transform motion-safe:duration-200 motion-safe:group-hover:scale-[1.02]" />
```
- Card hover: 1.02 zoom on photo (motion-safe, MICRO-03).
- Card lift (`hover:-translate-y-0.5`) on the outer Link is a separate, pre-existing transition.
- Initials placeholder branch does NOT zoom (no Image to scale).
```

Where in the file: replace the existing block from `## Component Specs` (line 130) THROUGH the end of the `### Modals` block (line 217). The next section (`## Style Guidelines`, line 218) stays.

**Sub-step 2B: Append `## v1.2 Overrides` section to OVERRIDES.md.**

Per CONTEXT.md "OVERRIDES.md format: table" + RESEARCH.md "Recommendation: Option 1 (append). Preserves all existing detail; new v1.2 deltas land in the requested table format; matches 'in-place edit' decision style."

Append AFTER the existing v1.0 numbered list (after the "Ratified By" footer at line 128-131 per RESEARCH.md), inserting BEFORE the trailing "Ratified By" if there is one to keep doc footers at the very end. Otherwise append at end of file.

```markdown
---

## v1.2 Overrides (Phases 13–15)

| Token / Component | v1.0 | v1.2 | Rationale |
|-------------------|------|------|-----------|
| `--text-4xl` | `2rem` (32 px) | `2.25rem` (36 px) | Inner-page H1 ≥1.2× above H2 (Phase 13-01) |
| `--text-5xl` | (n/a) | `2.5rem` (40 px) | Hero H1 cap; HomePage only (Phase 13-01) |
| `--color-ink-subtle` | `oklch(0.62 0.010 60)` | `oklch(0.45 0.012 60)` | WCAG AA 4.5:1 contrast on warm-cream (Phase 6-03) |
| Container widths | ad-hoc | max-w-3xl/5xl/6xl tiers | SPACE-01 layout rhythm (Phase 13-02) |
| Vertical rhythm | mixed | py-12 / py-16 / py-20 tiers | SPACE-02 (Phase 13-02) |
| Card padding | mixed | p-4 dense / p-6 spacious | SPACE-03 two-tier (Phase 13-03) |
| PersonCard width | flex-fill | max-w-[240px] w-full mx-auto | MEDIA-01 240 px cap (Phase 14-01) |
| PersonDetail hero | uncapped | 180 px × 225 px (4:5 portrait) | MEDIA-02 hero resize (Phase 14-02) |
| HeroCarousel pause/play focus ring | `ring-surface/70` | `ring-accent-ring + ring-offset-2 ring-offset-black/40` | BTN-02 unification + image-bg contrast (Phase 15-01) |
| HeroCarousel pause/play tap target | `w-6 h-6` (24×24) | `w-11 h-11` (44×44) | BTN-03 ≥44 (Phase 15-01) |
| HeroCarousel dot tap target | `w-2.5 h-2.5` + `gap-2` (~18 px center-to-center) | 10×10 visible inside `p-[17px]` button + `gap-0` (44 px center-to-center) | BTN-03 padding-inside pattern (Phase 15-01) |
| MobileNav trigger / close | `w-10 h-10` (40×40) | `w-11 h-11` (44×44) | BTN-01 sweep (Phase 15-02) |
| NavLink hit row | (no padding) | `py-1.5` + explicit `transition-colors duration-150` | BTN-05 + MICRO-01 (Phase 15-02) |
| LocaleToggle hit row | `px-2 py-1` | `px-2 py-1.5` | BTN-05 (Phase 15-02) |
| SourceFilter pill | `px-3 py-1` (~30 px) + no transition | `px-3.5 py-1.5` (~36 px) + `transition-colors duration-150` | BTN-04 + MICRO-02 (Phase 15-03) |
| PersonCard photo hover | (no transform on image) | `motion-safe:scale-[1.02] duration-200 group-hover` | MICRO-03 (Phase 15-04) |
| Focus ring offset | (no offset utility used) | `ring-offset-2 ring-offset-{surface|black/40}` everywhere | MICRO-05 macOS-style halo (Phase 15-01..05) |

**Inline-text link exemption:** WCAG 2.5.5 AAA inline-text exception applied to PartnerStrip, OutreachCard learn-more, SiteFooter EmailLink, ContactDetails social, SessionRow paper-link, PublicationEntry arXiv/DOI links, and PublicationEntry source-pill metadata chips. These deliberately do NOT receive 44×44 padding (per Phase 15 planner decision, RESEARCH open-question #1 + #3).

**Drift gate:** `.github/workflows/lint-rings.yml` runs `grep -EnP 'focus-visible:ring-(?!2($|\s|"|'\''|/|\\)|accent-ring|offset-)' src/` on push/PR to prevent reintroduction of stray ring colors.
```

Verify the table values against the actual final code (read 15-01..05 SUMMARYs first if any per-pixel decisions diverged from this plan — e.g., if 15-01 used `p-4` instead of `p-[17px]`, update the row accordingly).
  </action>
  <verify>
1. `cat design-system/cosmology-group-uba/MASTER.md | grep -A3 "## Component Specs"` returns the new "Universal Rules" subsection (NOT the old `.btn-primary` block).
2. `grep -c "ring-accent-ring" design-system/cosmology-group-uba/MASTER.md` returns ≥6 (one per recipe block).
3. `grep -n "## v1.2 Overrides" design-system/cosmology-group-uba/OVERRIDES.md` returns a line near end of file.
4. `grep -c "Phase 15-" design-system/cosmology-group-uba/OVERRIDES.md` returns ≥6 (multiple v1.2 phase-15 entries).
5. Existing v1.0 numbered overrides preserved: `grep -c "### " design-system/cosmology-group-uba/OVERRIDES.md` returns the same count as before this task ran (count v1.0 numbered entries first as baseline).
  </verify>
  <done>
MASTER.md `## Component Specs` rewritten as 6 Tailwind recipes + Universal Rules subsection. OVERRIDES.md has new `## v1.2 Overrides` table appended after the existing v1.0 numbered list (preserved). All values cross-checked against 15-01..05 SUMMARYs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final visual sweep at 375 / 1024 / 1440 px (MICRO-05 manual half)</name>
  <what-built>
Phases 15-01 through 15-06 land:
- HeroCarousel pause/play 44×44, dots 44×44 hit area with 10×10 visible chrome
- NavLink + LocaleToggle 44 px hit row inside h-24 header
- MobileNav trigger/close 44×44
- SourceFilter pills px-3.5 py-1.5 with smooth tone crossfade
- PersonCard photo zoom on hover (1.02, 200ms, motion-safe)
- All 19 focus-ring sites unified on `ring-accent-ring + ring-offset-{surface|black/40}`
- `pnpm axe`: 0 violations across all 8 Spanish pages
- CI drift gate active
- MASTER.md + OVERRIDES.md docs reflect v1.2

Per CONTEXT.md "MICRO-05 verification: manual viewport audit at 375 / 1024 / 1440 px — tab through Hero controls, Publications source-filter pills, People cards. Capture pass/fail per element. Same audit shape Phase 14-03 used. Does NOT rely on `pnpm axe` for ring contrast."
  </what-built>
  <how-to-verify>
1. **Start dev server:** `pnpm dev` (already running from Task 1, OR start fresh).
2. **Open `http://localhost:3000/es` in Chrome/Firefox.** DevTools → Responsive Mode → toggle through 375 / 1024 / 1440 px.
3. **Tab through HeroCarousel controls (pause/play + 3 dots):**
   - At each width, focus each control via Tab key.
   - Verify focus ring visible against the JWST starfield (the `ring-offset-black/40` halo should be clearly visible — accent ring + black offset reads as a glowing macOS-style focus halo).
   - Verify tap targets feel ≥44×44 (use Chrome's "Inspect" → check the rendered button bounding box).
4. **Navigate to `/es/publicaciones`. Tab through SourceFilter pills (Todas / arXiv / InspireHEP / Manual or whichever sources are present).**
   - Verify focus ring visible against light cream background.
   - Click a pill — observe smooth color crossfade (150ms, MICRO-02).
   - Pills should feel chunky enough (~36px tall).
5. **Navigate to `/es/personas`. Tab through PersonCard tiles. Hover each card with photo.**
   - Photo should subtly zoom (scale 1.02, 200ms ease-out).
   - Card lift (existing) should also fire — together "card lifts, photo zooms".
   - Test reduced motion: macOS System Settings → Accessibility → Display → Reduce motion ON, OR Chrome DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`. Verify zoom is GONE (motion-safe: filters it out).
6. **Tab through site header at 375px (mobile drawer trigger) and 1440px (desktop NavLinks + LocaleToggle).**
   - Mobile: trigger should feel ≥44×44, focus ring visible.
   - Desktop: NavLink hit row should feel ≥44 (use cursor near top/bottom of nav text — should still trigger hover).
7. **For each viewport, document PASS/FAIL per surface (HeroCarousel controls, SourceFilter pills, PersonCards, header).**

If any surface fails (e.g., focus ring invisible at 375px on PersonCard, photo zoom too aggressive, dots overlap), describe the issue precisely so a follow-up can route it.
  </how-to-verify>
  <resume-signal>Type "approved" if all surfaces pass at all 3 viewports, or describe specific failures with viewport + surface + screenshot path.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm axe` zero violations across 8 Spanish pages.
- Drift-gate workflow file exists and dry-runs clean against current src/.
- MASTER.md Component Specs section is Tailwind recipes (no raw `.btn-primary` blocks).
- OVERRIDES.md has new `## v1.2 Overrides` section in table form, preserving v1.0 list.
- Human visual sweep confirms PASS at 375 / 1024 / 1440 px.
- `pnpm build`, `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm check-translations` all green.
</verification>

<success_criteria>
1. `pnpm axe` script exists in package.json and runs cleanly against running dev server.
2. `.github/workflows/lint-rings.yml` exists and dry-run grep returns ZERO violations against current src/.
3. MASTER.md `## Component Specs` section uses Tailwind class strings, contains all 6 named components (button, pill, dot, nav link, locale toggle, carousel pause/play), and includes Universal Rules subsection with focus-ring + tap-target + motion rules.
4. OVERRIDES.md has `## v1.2 Overrides` table with ≥15 rows covering Phases 13–15, AND existing v1.0 numbered list is preserved.
5. Manual visual sweep approved by user at all 3 viewports.
6. Drift-gate regex confirms zero violations.
</success_criteria>

<output>
After completion, create `.planning/phases/15-interactive-polish-documentation/15-06-SUMMARY.md` documenting:
- `pnpm axe` invocation used (with or without CHROME_TEST_PATH workaround), output URL summary, violation count
- `lint-rings.yml` workflow file path + final regex used
- MASTER.md Component Specs replacement scope (lines replaced)
- OVERRIDES.md insertion location + row count
- Human visual sweep result per viewport per surface
- Atomic commits made: `feat(15-06): add pnpm axe script + lint-rings CI gate`, `docs(15-06): rewrite MASTER.md Component Specs as Tailwind recipes`, `docs(15-06): append v1.2 overrides table to OVERRIDES.md`
</output>
