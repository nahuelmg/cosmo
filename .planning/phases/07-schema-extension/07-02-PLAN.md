---
phase: 07-schema-extension
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - content/people.json
autonomous: false

must_haves:
  truths:
    - "Every current member in content/people.json has a populated display_name_normalized matching the normalizeName spec"
    - "Every current PI and postdoc has a populated inspirehep_id (BAI format)"
    - "Every current PI and postdoc has a populated arxiv_id — or the field is intentionally absent with a recorded reason"
    - "Every current PhD has a populated inspirehep_id and arxiv_id — or the field is intentionally absent with a recorded reason"
    - "Undergraduates have display_name_normalized only (inspirehep_id / arxiv_id optional; left out is fine)"
    - "pnpm validate-content passes on the updated people.json — no Zod errors"
    - "Adding a test inspirehep_id value like 'E.Calzetta.1' to any entry would continue to pass validation (schema tolerance confirmed)"
  artifacts:
    - path: "content/people.json"
      provides: "All current members have display_name_normalized + investigators/postdocs/PhDs have IDs"
      contains: "display_name_normalized"
  key_links:
    - from: "content/people.json"
      to: "src/content/schemas/people.schema.ts"
      via: "parsed by validate-content script through PeopleSchema.safeParse"
      pattern: "PeopleSchema"
    - from: "content/people.json"
      to: "content/SYNC.md"
      via: "maintainer followed SYNC.md lookup steps for each BAI / arXiv author ID"
      pattern: "inspirehep_id|arxiv_id"

user_setup:
  - service: inspirehep
    why: "Populate inspirehep_id (BAI) for investigators, postdocs, and PhDs"
    env_vars: []
    dashboard_config:
      - task: "Look up each member's BAI on inspirehep.net/authors (per content/SYNC.md)"
        location: "https://inspirehep.net/authors"
  - service: arxiv
    why: "Populate arxiv_id (claimed author ID) for investigators, postdocs, and PhDs"
    env_vars: []
    dashboard_config:
      - task: "Look up each member's claimed arXiv author ID on arxiv.org/user (per content/SYNC.md)"
        location: "https://arxiv.org/user"
---

<objective>
Populate the three new person fields (`display_name_normalized`, `inspirehep_id`, `arxiv_id`) for every current member in `content/people.json`, so that:
1. `pnpm validate-content` passes (the required `display_name_normalized` field is satisfied for every entry — the remaining blocker from Plan 07-01).
2. Phase 9's sync script can later query InspireHEP and arXiv for every PI, postdoc, and PhD without hitting missing-ID warnings.

Purpose: Close DATA-09 (inspirehep_id coverage) and DATA-10 (arxiv_id coverage) for the v1.1 milestone, plus the CONTEXT.md-mandated display_name_normalized coverage. This is a human-action plan: the maintainer supplies real BAI identifiers and arXiv author IDs via checkpoint; Claude does the JSON editing and validation.

Output: `content/people.json` with all three fields populated on appropriate entries; `pnpm validate-content` green.

Scope note (from CONTEXT.md): Students (undergrads) are DEFERRED — schema `inspirehep_id` and `arxiv_id` are both optional for undergrads, so leaving them out is fine. Undergrads still need `display_name_normalized` because that field is required on every entry. Past members are UNCHANGED — this plan touches only CURRENT members (`category` in `pi`, `postdoc`, `phd`, `undergrad`).
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-schema-extension/07-CONTEXT.md

# Plan 07-01 output — schema + maintainer guide the human follows
@.planning/phases/07-schema-extension/07-01-SUMMARY.md
@content/SYNC.md

# Schemas this plan validates against
@src/content/schemas/people.schema.ts
@src/content/schemas/shared.ts

# File this plan modifies
@content/people.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute display_name_normalized for every current member and patch content/people.json</name>
  <files>content/people.json</files>
  <action>
    Populate the required `display_name_normalized` field on EVERY current member in `content/people.json` (categories: `pi`, `postdoc`, `phd`, `undergrad`). Past members (`category: "past"`) are OUT OF SCOPE — leave them alone.

    **Step 1.** Read `content/people.json` and enumerate entries. As of the plan writing, the current roster is:

    - `esteban-calzetta` (pi) — "Esteban Calzetta"
    - `diana-lopez-nacir` (pi) — "Diana Lopez Nacir"
    - `susana-landau` (pi) — "Susana Landau"
    - `cecilia-scannapieco` (pi) — "Cecilia Scanapiecco"
    - `nahuel-miron-granese` (pi) — "Nahuel Miron Granese"
    - `juan-manuel-armaleo` (postdoc) — "Juan Manuel Armaleo"
    - `javier-badia` (postdoc) — "Javier Badia"
    - `tomas-ferreira-chase` (phd) — "Tomas Ferreira Chase"
    - `matias-leizerovitch` (phd) — "Matias Leizerovitch"
    - `gonzalo-santa-cruz` (phd) — "Gonzalo Santa Cruz"
    - `augusto-chantada` (phd) — "Augusto Chantada"
    - `guadalupe-ahumada-acuna` (phd) — "Guadalupe Ahumada Acuña"
    - `juan-pablo-elia` (phd) — "Juan Pablo Elia"
    - `javier-pineau` (undergrad) — "Javier Pineau"
    - `tomas-cicarella` (undergrad) — "Tomas Cicarella"

    Re-derive this list at execution time by grepping the file — the source of truth is the current `people.json`, not this list (someone may have added/removed entries since).

    **Step 2.** For each current member, compute `display_name_normalized` by applying `normalizeName(name)` as defined in `src/content/schemas/shared.ts`:
    - NFD decomposition
    - Strip combining marks (`\p{M}`)
    - Lowercase

    Use the exported helper directly, e.g. via a one-shot `tsx -e` script to avoid hand-typing errors on accented names:

    ```bash
    pnpm exec tsx -e '
      import { normalizeName } from "./src/content/schemas/shared.ts";
      import people from "./content/people.json" with { type: "json" };
      for (const p of people) {
        if (p.category === "past") continue;
        console.log(JSON.stringify({ slug: p.slug, name: p.name, normalized: normalizeName(p.name) }));
      }
    '
    ```

    Record the normalized values (for example, `"Guadalupe Ahumada Acuña"` → `"guadalupe ahumada acuña"` first, then after NFD + strip combining marks → `"guadalupe ahumada acuna"`). Verify visually that diacritics are stripped in the output.

    **Step 3.** Insert `display_name_normalized` into each current member's JSON entry. Per research Q10, the field belongs AFTER `publications_selected` and BEFORE `contact`. For a PI entry without any IDs yet, this looks like:

    ```json
    {
      ...
      "publications_selected": [],
      "display_name_normalized": "esteban calzetta",
      "contact": { ... },
      ...
    }
    ```

    Task 2 will add `inspirehep_id` and `arxiv_id` ABOVE `display_name_normalized` (also between `publications_selected` and `contact`). For now, only `display_name_normalized` is inserted on every current member.

    **Step 4.** Validate immediately:

    ```bash
    pnpm validate-content
    ```

    If the schema passes, commit: `data(07-02): populate display_name_normalized on current members`.

    If it fails, the failure MUST only be about `display_name_normalized` being empty/missing on a specific entry (i.e. a member we missed) OR about unrelated pre-existing data. If the failure cites a different new field, STOP — that's a schema-consumer bug, not a data bug.

    Do NOT touch `category: "past"` entries. Their `display_name_normalized` absence is ignored because — wait, actually, re-read the schema: `display_name_normalized` is required on the PersonSchema level, with no `category`-conditional `superRefine` exemption. If `pnpm validate-content` fails on past members, we have two options:

    - **Option A (preferred, consistent with CONTEXT.md scope):** Populate `display_name_normalized` on past members too — it's trivial (one `normalizeName(name)` call per past entry) and it keeps the schema clean. No ID fields needed for past members.
    - **Option B:** Add a `superRefine` exemption for past members.

    **DECISION for execution:** Go with Option A. It's 3 minutes of JSON editing and keeps the schema simple. Apply `display_name_normalized` to past members too in this task.

    So: every entry in `content/people.json` gets `display_name_normalized`, current AND past. Only IDs differ (Task 2 covers current members only).
  </action>
  <verify>
    1. `jq '[.[] | select(.display_name_normalized == null)]' content/people.json` returns `[]` (no entry missing the field).
    2. `jq -r '.[] | [.slug, .display_name_normalized] | @tsv' content/people.json` shows normalized values for every entry; spot-check that `guadalupe-ahumada-acuna` has `"guadalupe ahumada acuna"` (no combining marks).
    3. `pnpm validate-content` exits 0.
    4. `git diff content/people.json` shows only insertions of the `display_name_normalized` line; no other fields changed.
  </verify>
  <done>
    - Every entry (current + past) has `display_name_normalized` populated with NFD + strip + lowercase of `name`.
    - `pnpm validate-content` passes.
    - Field inserted between `publications_selected` and `contact` (per research Q10 positioning).
    - Committed: `data(07-02): populate display_name_normalized on all members`.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Collect inspirehep_id + arxiv_id values from maintainer</name>
  <decision>
    For each current PI, postdoc, and PhD, the maintainer must provide either:
    (a) the real BAI (`inspirehep_id`) and the real claimed arXiv author ID (`arxiv_id`), OR
    (b) the string `"skip"` with a brief reason (e.g. "no InspireHEP profile yet", "hasn't claimed papers on arXiv").

    Undergraduates (Javier Pineau, Tomas Cicarella) are optional — maintainer can skip both fields silently.

    The executor uses the values to populate `content/people.json` in Task 3.
  </decision>
  <context>
    This is the DATA-09 / DATA-10 collection gate. The executor needs real identifiers it cannot derive — each BAI must be looked up on inspirehep.net/authors, and each arXiv author ID must be retrieved from each researcher's arxiv.org account page. Only the maintainer (PI / group admin) has the authority and the access to compile these values.

    Per content/SYNC.md: BAI format is `Initial.Surname.N` (e.g. `E.Calzetta.1`); arXiv author ID is an opaque string from the researcher's arxiv.org account.

    Coverage target (from CONTEXT.md):
    - 100% of current PIs, postdocs, PhDs should have BOTH fields populated or explicitly skipped with a reason.
    - Undergrads: optional, silent skip fine.

    Policy for missing IDs (CONTEXT.md: "skip silently + log INFO in sync script"): the Phase 9 sync script will skip silently and log INFO when it encounters a missing ID. Phase 7 just needs the schema-level data (populated or absent).

    There is NO calendar deadline — the maintainer can complete this gate any time before Phase 9 end-to-end testing. Per CONTEXT.md: "does NOT block Phase 7 completion; blocks Phase 9 E2E test only." In practice, if the maintainer wants to unblock the phase immediately, they can submit `"skip"` for all unknown members and fill them in later via a follow-up data commit — the schema tolerates missing optional IDs.
  </context>
  <options>
    <option id="complete">
      <name>Provide all values now</name>
      <pros>Phase 9 sync script runs against a fully-populated roster on first execution; no follow-up data commit needed.</pros>
      <cons>Requires the maintainer to sit down with inspirehep.net + arxiv.org and look up ~13 pairs of IDs in one session.</cons>
    </option>
    <option id="partial">
      <name>Provide values for the members you know now; skip the rest</name>
      <pros>Unblocks Phase 7 immediately. Skipped members can be filled in later via a data-only follow-up commit.</pros>
      <cons>Phase 9 sync will log INFO "skipping inspirehep for <name>" for each skipped member until they're filled in.</cons>
    </option>
    <option id="skip-all">
      <name>Skip all IDs now; fill in later</name>
      <pros>Fastest gate crossing; lets the phase complete with only the schema change.</pros>
      <cons>Phase 9's first real run will skip almost every author lookup. Not recommended unless the maintainer is rushed.</cons>
    </option>
  </options>
  <resume-signal>
    Provide values as a YAML block, keyed by slug. For each current PI / postdoc / PhD, give either real IDs or `"skip"` with a reason. Undergrads can be omitted entirely. Example:

    ```yaml
    esteban-calzetta:
      inspirehep_id: "E.Calzetta.1"
      arxiv_id: "calzetta_e_1"
    diana-lopez-nacir:
      inspirehep_id: "D.LopezNacir.1"
      arxiv_id: "skip"
      arxiv_skip_reason: "never claimed papers on arxiv"
    susana-landau:
      inspirehep_id: "skip"
      inspirehep_skip_reason: "no inspirehep profile"
      arxiv_id: "skip"
      arxiv_skip_reason: "never claimed papers on arxiv"
    # ... remaining members ...
    ```

    Any entry omitted from the YAML is treated as "skip both IDs" (same as option `skip-all` for that member).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply IDs to content/people.json and verify SCHEMA-06 green on full content</name>
  <files>content/people.json</files>
  <action>
    Using the YAML block the maintainer submitted at the checkpoint:

    **Step 1.** For each keyed slug, locate the corresponding entry in `content/people.json`.
    - If `inspirehep_id` is a real BAI value (not `"skip"`): insert `"inspirehep_id": "<value>"` between `publications_selected` and `display_name_normalized`.
    - If `inspirehep_id` is `"skip"` or missing: do NOT add the field (schema is optional).
    - Same rule for `arxiv_id`.

    Final field order for a populated entry (per research Q10):

    ```json
    {
      ...
      "publications_selected": [],
      "inspirehep_id": "E.Calzetta.1",
      "arxiv_id": "calzetta_e_1",
      "display_name_normalized": "esteban calzetta",
      "contact": { ... },
      ...
    }
    ```

    If only one of the two is populated, include only that one:

    ```json
    "publications_selected": [],
    "inspirehep_id": "E.Calzetta.1",
    "display_name_normalized": "esteban calzetta",
    "contact": { ... }
    ```

    Preserve all existing fields unchanged (do not rewrite `full_bio`, `research_interests`, etc.). Keep formatting consistent with the file's existing 2-space indentation.

    **Step 2.** After all maintainer-provided values are inserted, do a final format-validation pass. For any `inspirehep_id` the maintainer provided, confirm it matches `/^[A-Z]\.[A-Za-z-]+\.\d+$/`. For any `arxiv_id`, confirm it matches `/^(\d{4}\.\d{4,5}|[a-z-]+\/\d{7})(v\d+)?$/`.

    **NOTE on arxiv_id vs arxiv author ID:** The `arxivId` helper from shared.ts validates arXiv PAPER IDs (e.g. `2501.12345`, `gr-qc/9209007`), not arXiv AUTHOR IDs (which are opaque strings like `calzetta_e_1`). Research Q8 reuses the `arxivId` helper for `arxiv_id` for simplicity, but actual arXiv author IDs will FAIL this regex because they don't look like paper IDs.

    If the maintainer's arxiv_id value doesn't match the paper-ID regex, **STOP and escalate** — this is a schema bug surfaced by real data. The fix is either:
    (a) Loosen the regex in `shared.ts` (e.g. define a separate `arxivAuthorId` helper like `/^[a-z0-9_-]+$/i`), OR
    (b) The maintainer supplied the wrong format (arXiv paper IDs, not author IDs).

    Do NOT silently edit `shared.ts` — escalate to the user with a summary of which slug's value failed and ask whether to (a) fix the schema or (b) request a different format from the maintainer. This is a known open issue from the research phase that only surfaces when real IDs arrive.

    **Step 3.** Run the full validation suite:

    ```bash
    pnpm validate-content
    pnpm typecheck
    ```

    Both must exit 0.

    **Step 4.** Sanity check — confirm SC2 of the phase: add `"inspirehep_id": "E.Calzetta.1"` to a person entry that currently lacks it (or spot check an entry that has a real BAI) and re-run `pnpm validate-content` — it must still pass. This proves the strictObject doesn't reject the field.

    Commit: `data(07-02): populate inspirehep_id and arxiv_id for current members (DATA-09 + DATA-10)`

    If some members were skipped with reasons, record the skip reasons in the SUMMARY (not in people.json — the schema doesn't store the reason).
  </action>
  <verify>
    1. `pnpm validate-content` exits 0.
    2. `pnpm typecheck` exits 0.
    3. `jq '[.[] | select(.category == "pi" or .category == "postdoc") | {slug, has_bai: (.inspirehep_id != null), has_arxiv: (.arxiv_id != null)}]' content/people.json` — inspect coverage; each PI and postdoc has `has_bai` or `has_arxiv` true unless explicitly skipped.
    4. `jq '[.[] | select(.inspirehep_id != null) | .inspirehep_id] | unique' content/people.json` — all BAI values match `/^[A-Z]\.[A-Za-z-]+\.\d+$/`.
    5. `git diff content/people.json` shows only insertions of the three new fields on appropriate entries. No unrelated diffs.
    6. Spot-check: open `content/people.json` in VS Code; hover over an inspirehep_id value — autocomplete via `content/people.schema.json` recognizes the field.
  </verify>
  <done>
    - All maintainer-provided IDs inserted into `content/people.json`.
    - Every PI, postdoc, and PhD has the fields populated OR explicitly skipped (skip reasons recorded in SUMMARY).
    - All values pass schema regex validation.
    - `pnpm validate-content` + `pnpm typecheck` green.
    - SC2 of the phase explicitly verified (adding an `inspirehep_id` to an entry does not trigger a strictObject rejection).
    - Committed: `data(07-02): populate inspirehep_id + arxiv_id (DATA-09 + DATA-10)`.
  </done>
</task>

</tasks>

<verification>
End-of-phase whole-file checks:

1. `pnpm validate-content` — exits 0 on the final committed `content/people.json`.
2. `pnpm typecheck` — exits 0.
3. `pnpm build` (optional, stronger) — full Next.js build succeeds end-to-end (prebuild runs validate-content, so this subsumes check 1).
4. `jq '[.[] | select(.display_name_normalized == null)]' content/people.json` returns `[]`.
5. Review the SUMMARY's "skipped members" table — it lists any member without IDs and why.

Phase success criterion verification mapping:
- **SC1** (pnpm validate-content passes on v1.0 publications.json): covered by Plan 07-01 Task 2 Step 3.
- **SC2** (inspirehep_id "E.Calzetta.1" is accepted): covered by Task 3 Step 4.
- **SC3** (pre-2007 arXiv ID like gr-qc/9209007 passes): covered by Plan 07-01 Task 1 Edit 1a + Task 2 inline parse check.
- **SC4** (JSON Schemas regenerated in same commit as Zod): covered by Plan 07-01 Tasks 1+2.
- **SC5** (publications_selected @deprecated): covered by Plan 07-01 Task 1 Edit 3b.

DATA-09 / DATA-10 coverage:
- All PIs + postdocs + PhDs either have both IDs OR have a recorded skip reason in the SUMMARY.
- Undergrads optional (schema-tolerant).
- Past members: display_name_normalized only (for schema consistency).
</verification>

<success_criteria>
- [ ] `content/people.json` has `display_name_normalized` on every entry (current + past).
- [ ] Every current PI and postdoc has `inspirehep_id` populated OR a skip reason recorded in SUMMARY.
- [ ] Every current PI and postdoc has `arxiv_id` populated OR a skip reason recorded in SUMMARY.
- [ ] Every current PhD has `inspirehep_id` + `arxiv_id` populated OR skip reasons recorded in SUMMARY.
- [ ] Undergrads: `display_name_normalized` populated; IDs optional.
- [ ] All BAI values match `/^[A-Z]\.[A-Za-z-]+\.\d+$/`.
- [ ] `pnpm validate-content` and `pnpm typecheck` both exit 0.
- [ ] `content/people.json` diff is insert-only (no field reordering, no unrelated edits).
- [ ] Requirements DATA-09 + DATA-10 satisfied (or skip reasons explicitly logged — acceptable per CONTEXT.md: "does not block Phase 7").
- [ ] SC2 of the phase verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-extension/07-02-SUMMARY.md`. Key frontmatter/content:
- `affects: [09-sync-script]` — Phase 9 reads these IDs at sync time.
- `patterns.established`: "per-person external-ID population workflow", "skip-with-reason policy for missing IDs".
- `decisions`: record which members were skipped and why (short table), and note whether the arxiv_id regex had to be loosened (if Task 3 escalation fired).
- `blockers_resolved`: DATA-09 (inspirehep_id populated), DATA-10 (arxiv_id populated) — for the members who had IDs available.
- `blockers_remaining`: list of members still awaiting IDs (if any) — these roll forward as a Phase 9 pre-E2E-test checklist.
</output>
