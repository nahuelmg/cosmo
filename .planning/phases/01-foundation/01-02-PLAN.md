---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/cosmology-group-uba/MASTER.md
  - design-system/cosmology-group-uba/OVERRIDES.md
autonomous: false

must_haves:
  truths:
    - "design-system/cosmology-group-uba/MASTER.md exists with a complete ui-ux-pro-max design system"
    - "Palette is warm-academic (hues in the 30°–80° band for accent); no cool-institutional blues, no neon, no dark-mode-first"
    - "Typography pairing is serif display + sans body; both families ship Latin + Latin-Extended + Greek subsets"
    - "No border tokens or divider hairline rules are prescribed; surface hierarchy is whitespace-only + one alternate surface"
    - "Every deviation from ui-ux-pro-max raw output is recorded in OVERRIDES.md with rationale citing CONTEXT.md"
  artifacts:
    - path: "design-system/cosmology-group-uba/MASTER.md"
      provides: "Locked design system — palette, typography, spacing, shadows, style rules"
      min_lines: 80
    - path: "design-system/cosmology-group-uba/OVERRIDES.md"
      provides: "Record of manual edits to raw ui-ux-pro-max output and why"
      min_lines: 15
  key_links:
    - from: "MASTER.md palette"
      to: "warm-academic band"
      via: "accent hue 30°–80°"
      pattern: "oklch\\([^)]*\\s(3[0-9]|[4-7][0-9]|80)\\b"
    - from: "MASTER.md typography"
      to: "Greek subset support"
      via: "explicit subset note"
      pattern: "(Greek|greek)"
    - from: "OVERRIDES.md"
      to: "CONTEXT.md constraints"
      via: "citation"
      pattern: "CONTEXT\\.md"
---

<objective>
Generate the project's locked design system via `ui-ux-pro-max`, then validate and, where necessary, override its output against the CONTEXT.md constraints (warm-academic, no borders, Greek subset, restrained type scale) so that Plan 01-04 can mechanically translate MASTER.md into OKLCH tokens in `globals.css`.

Purpose: Establish FOUND-02 — the design system is materialised as a reviewable Markdown source of truth. RESEARCH.md's Open Questions #1–3 explicitly call for a planner-enforced override gate: raw tool output cannot be trusted to respect the "no borders" / warm-palette / Greek-subset constraints.

Output:
- `design-system/cosmology-group-uba/MASTER.md` — the ui-ux-pro-max output, manually corrected against CONTEXT.md.
- `design-system/cosmology-group-uba/OVERRIDES.md` — a diff-style log of every change made to the raw tool output, each entry citing the CONTEXT.md line that motivated it.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@skills/design/README.md
@skills/design/ui-ux-pro-max/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ui-ux-pro-max with warm-academic query, land raw output in MASTER.md</name>
  <files>
    design-system/cosmology-group-uba/MASTER.md
  </files>
  <action>
Run the ui-ux-pro-max design-system generator with a query that encodes CONTEXT.md's palette, typography, and mood constraints, and persist the output to the project's design-system folder:

```bash
python3 skills/design/ui-ux-pro-max/scripts/search.py \
  "academic institutional scholarly research warm minimal serif typography whitespace dense editorial nature max-planck" \
  --design-system --persist \
  -p "Cosmology Group UBA"
```

Expected output path: `design-system/cosmology-group-uba/MASTER.md`.

If the script writes elsewhere (e.g. `./MASTER.md` or `design-system/MASTER.md` without the product subfolder), relocate it to `design-system/cosmology-group-uba/MASTER.md` so the path matches `ROADMAP.md`'s success criterion #2 and RESEARCH.md's project-structure diagram.

Do NOT edit the file yet. Leave the raw output intact so Task 2 can diff against it.

Immediately after the script runs, briefly inspect the MASTER.md output and note (in scratch, not yet in OVERRIDES.md) every field that violates CONTEXT.md:
- Palette: any cool institutional blue, neon accent, gradient, dark-mode-first background, or >2 surface tiers.
- Typography: all-sans pairings, sans-display + serif-body inversions, families without Greek subset (check `skills/design/ui-ux-pro-max/data/google-fonts.csv` for `greek` in the `subsets` column), heavy display weights.
- Effects: border/divider tokens, hairline rules, heavy shadow recipes (shadows up to `sm`/`md` are acceptable; `xl` with dark halos is not).
- Scale: display sizes above ~32px for H1, ratio above 1.25.
  </action>
  <verify>
```bash
test -f design-system/cosmology-group-uba/MASTER.md
wc -l design-system/cosmology-group-uba/MASTER.md   # expect at least 60 lines of output
head -40 design-system/cosmology-group-uba/MASTER.md | grep -iE "(palette|typography|color|font)"
```
  </verify>
  <done>
- `design-system/cosmology-group-uba/MASTER.md` exists with a complete ui-ux-pro-max design-system output (palette, typography, spacing, shadows, component specs, guidelines).
- The file is the raw, unmodified tool output — corrections happen in the next two tasks.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Review MASTER.md against CONTEXT.md and decide overrides</name>
  <decision>
Which specific fields of `design-system/cosmology-group-uba/MASTER.md` need to be overridden to satisfy the CONTEXT.md constraints? Present the user with a concrete override proposal and confirm it before writing to disk.
  </decision>
  <context>
RESEARCH.md Open Questions #1–3 explicitly flag that raw ui-ux-pro-max output frequently violates the project's constraints:

- **"No borders" (CONTEXT.md > Palette character):** tool may emit `--border-*`, card hairlines, section dividers.
- **Warm-academic band (CONTEXT.md > Palette character):** tool may emit cool-institutional blues, neon accents, UBA-blue.
- **Greek subset required on BOTH families (CONTEXT.md > Font delivery):** tool may propose Crimson Pro, Atkinson Hyperlegible, or other Greek-less families. RESEARCH.md's primary recommendation is **Source Serif 4 + Source Sans 3** (both OFL, both variable, both verified Greek on Google Fonts).
- **Restrained type scale (CONTEXT.md > Typography pairing):** H1 should be 28–32px, ratio 1.2–1.25. Tool may propose hero-splash scale.
- **Background ladder (CONTEXT.md > Palette character):** exactly TWO surfaces (primary + one alternate). Tool may emit three tiers.

Produce a review table of the form:

| Field in MASTER.md | Raw value | CONTEXT.md constraint | Decision |
|--------------------|-----------|-----------------------|----------|
| Accent colour | e.g. `oklch(0.55 0.18 250)` (cool blue) | Warm band (30°–80°) | Override to muted terracotta `oklch(0.52 0.12 45)` |
| Heading font | e.g. "Crimson Pro" | Serif + Greek subset | Override to "Source Serif 4" |
| Body font | e.g. "Atkinson Hyperlegible" | Sans + Greek subset | Override to "Source Sans 3" |
| Border radius tokens | `--border-*` present | No borders | Remove border tokens entirely |
| Surface tiers | 3 tiers | 2 tiers (primary + alt) | Drop third tier |
| Type scale ratio | e.g. 1.333 | 1.2–1.25 | Retune H1…H4 to ratio 1.2 |
| Shadow `--shadow-xl` | Dark halo | No borders implies restrained shadows | Drop `xl` and `2xl`, keep `sm`/`md` only |

Present this table to the user. If the raw output already satisfies CONTEXT.md (rare), present a single-row table saying "No overrides needed" and ask for confirmation.
  </context>
  <options>
    <option id="approve-overrides">
      <name>Approve the proposed override list</name>
      <pros>Locks the design system against CONTEXT.md; unblocks Plan 01-04 immediately.</pros>
      <cons>Commits to a set of decisions that may need revisiting if a downstream plan reveals a mismatch.</cons>
    </option>
    <option id="modify-overrides">
      <name>Modify specific overrides</name>
      <pros>User can hand-tune the palette or fonts (e.g. prefer EB Garamond + Inter over Source Serif 4 + Source Sans 3).</pros>
      <cons>Requires the user to specify the alternative value; slightly slower to converge.</cons>
    </option>
    <option id="rerun-tool">
      <name>Re-run ui-ux-pro-max with a different query</name>
      <pros>Cheaper to re-query than to hand-edit if the raw output is badly misaligned.</pros>
      <cons>May not converge on a warm-academic palette; fonts still have to be validated by hand for Greek.</cons>
    </option>
  </options>
  <resume-signal>
Respond with:
- `approve-overrides` to accept the proposed table as-is.
- `modify-overrides: <field> = <new value>` (one or more) to hand-edit specific cells.
- `rerun-tool: <new query>` to re-run `search.py` with a different prompt and repeat the review.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply overrides to MASTER.md and record them in OVERRIDES.md</name>
  <files>
    design-system/cosmology-group-uba/MASTER.md
    design-system/cosmology-group-uba/OVERRIDES.md
  </files>
  <action>
Apply every override approved in Task 2 to `design-system/cosmology-group-uba/MASTER.md`. Edit the Markdown in place — rewrite palette rows, swap font family names and Google Fonts URLs, remove border-radius tokens, retune type scale, drop excessive shadow tiers. Keep the file's original section headings so it still reads as a ui-ux-pro-max output.

**Font policy (non-negotiable regardless of user input, unless user explicitly picked a different Greek-ready pair):**
- Heading font: **Source Serif 4** (variable, OFL, Greek subset verified).
- Body font: **Source Sans 3** (variable, OFL, Greek subset verified, weights 400/600/700).
- Google Fonts URL field in MASTER.md should reference these families.
- If user selected the EB Garamond + Inter fallback in Task 2, use that pair — both also ship Greek.

**Palette policy:**
- Primary surface: `oklch(0.995 0.003 85)` (warm ivory, hue 85 — edge of warm band).
- Alternate surface: `oklch(0.978 0.008 80)` (slightly deeper ivory).
- Ink (body text): `oklch(0.22 0.015 60)` (warm near-black).
- Ink muted: `oklch(0.48 0.012 60)`.
- Ink subtle: `oklch(0.62 0.010 60)`.
- Accent: one warm hue in the 30°–80° band; default `oklch(0.52 0.12 45)` (muted terracotta) unless user overrode in Task 2.
- NO border colours. If the raw output had `--border-*` tokens, delete the whole row.

**Type scale:**
- Body size: 15–16px (use `0.9375rem` for `--text-sm`, `1rem` for `--text-base` with `line-height: 1.5`).
- Ratio 1.2 → H1 ≈ `2rem` (32px), H2 ≈ `1.625rem`, H3 ≈ `1.375rem`, H4 ≈ `1.125rem`.
- Weights: serif display at 600 semibold; sans body at 400 regular / 600 semibold / 700 bold.

**Write `design-system/cosmology-group-uba/OVERRIDES.md`** capturing every change. Template:

```markdown
# Design System — Overrides Against Raw ui-ux-pro-max Output

**Source:** `design-system/cosmology-group-uba/MASTER.md` (ui-ux-pro-max generated 2026-04-17)
**Reviewer:** Phase 1, Plan 01-02
**Constraints source:** `.planning/phases/01-foundation/01-CONTEXT.md`

## Overrides Applied

### 1. Accent colour
- **Raw:** `<raw value from tool>`
- **Override:** `oklch(0.52 0.12 45)` (muted terracotta)
- **Why:** CONTEXT.md > Palette character — "Warm-academic direction (Nature / Oxford leaning) — NOT cool-institutional, NOT UBA-blue-forward." Raw value was in the cool band (hue > 200°). Enforced warm band 30°–80°.

### 2. Heading font
- **Raw:** `<raw family>`
- **Override:** Source Serif 4 (variable, OFL, Greek subset)
- **Why:** CONTEXT.md > Font delivery & subsets — Greek subset required for Λ Ω H₀ σ₈. Raw family does not ship Greek on Google Fonts (cross-referenced `skills/design/ui-ux-pro-max/data/google-fonts.csv`).

### 3. Body font
- **Raw:** `<raw family>`
- **Override:** Source Sans 3 (variable, OFL, Greek subset)
- **Why:** Same as #2 — Greek subset mandate for both families so Greek letters don't switch visual style mid-sentence.

### 4. Border tokens
- **Raw:** `--border-*` tokens present
- **Override:** Removed
- **Why:** CONTEXT.md > Palette character — "No borders. Hierarchy carried by typography and whitespace alone."

### 5. Surface tiers
- **Raw:** 3 tiers
- **Override:** 2 tiers (primary + alt)
- **Why:** CONTEXT.md > Palette character — "Background ladder: white primary + ONE subtle alternate surface."

### 6. Type scale
- **Raw ratio:** `<raw ratio>`, H1 `<raw size>`
- **Override:** Ratio 1.2, H1 2rem (32px), body 1rem with line-height 1.5
- **Why:** CONTEXT.md > Typography pairing — "Body feel: dense and compact (15–16px, ~1.45–1.5 line-height). Display scale: restrained (ratio 1.2–1.25, H1 28–32px)."

### 7. Shadow tiers
- **Raw:** `sm` through `2xl`
- **Override:** `sm` + `md` only
- **Why:** CONTEXT.md > Palette character — restraint follows from the no-borders / whitespace-hierarchy principle; large halo shadows contradict the Nature-long-form aesthetic.

## Ratified By
- Plan 01-02 Task 2 checkpoint (2026-04-17)
- Accepted by user: `approve-overrides` | `modify-overrides: …`
```

Populate each `<raw>` placeholder with the literal value MASTER.md had before the edit. If a given override wasn't necessary (raw output already satisfied a constraint), omit that entry and note it in a closing "No override needed" section.
  </action>
  <verify>
```bash
# MASTER.md still exists and now reflects the overrides
test -f design-system/cosmology-group-uba/MASTER.md
grep -iE "(Source Serif 4|EB Garamond|Noto Serif)" design-system/cosmology-group-uba/MASTER.md
grep -iE "Greek" design-system/cosmology-group-uba/MASTER.md

# No border tokens survived
! grep -E "^\s*--border[- ]" design-system/cosmology-group-uba/MASTER.md

# Accent is in warm band (hue 30–80). Grep an oklch with trailing hue in that band.
grep -E "oklch\([^)]*\s(3[0-9]|[4-7][0-9]|80)\)" design-system/cosmology-group-uba/MASTER.md

# OVERRIDES.md exists and cites CONTEXT.md
test -f design-system/cosmology-group-uba/OVERRIDES.md
grep -q "CONTEXT.md" design-system/cosmology-group-uba/OVERRIDES.md
```
  </verify>
  <done>
- `design-system/cosmology-group-uba/MASTER.md` reflects warm-academic palette, Greek-subset serif+sans pair (Source Serif 4 + Source Sans 3 by default), no border tokens, two surface tiers, restrained type scale, restrained shadow tiers.
- `design-system/cosmology-group-uba/OVERRIDES.md` records every edit with raw value, override value, and CONTEXT.md citation.
- Plan 01-04 can now translate MASTER.md mechanically into `globals.css` OKLCH tokens.
  </done>
</task>

</tasks>

<verification>
1. `ls design-system/cosmology-group-uba/` shows `MASTER.md` and `OVERRIDES.md`.
2. `MASTER.md` names Source Serif 4 + Source Sans 3 (or user-chosen alternative Greek-ready pair) as the typography.
3. `MASTER.md` has NO `--border-*` tokens.
4. `MASTER.md` accent is an OKLCH triple with hue in 30°–80°.
5. `OVERRIDES.md` cites `CONTEXT.md` and lists every raw→override change with rationale.
</verification>

<success_criteria>
- Design system locked; subsequent plans can cite `design-system/cosmology-group-uba/MASTER.md` as the single source of truth for tokens.
- Every deviation from the raw `ui-ux-pro-max` output is recorded, not silent.
- User-visible decision point (the override list) has been explicitly ratified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` using the template at `/home/tomas/.claude/get-shit-done/templates/summary.md`. Fill the frontmatter with:
- `subsystem: foundation/design-system`
- `affects: [01-04]`
- `tech-stack.added: [Source Serif 4, Source Sans 3]` (or user's alternative)
- Key files: `design-system/cosmology-group-uba/MASTER.md`, `design-system/cosmology-group-uba/OVERRIDES.md`
- Decisions: the ratified palette hue, serif/sans families, no-border policy, two-tier surface policy, type scale ratio.
</output>
</content>
</invoke>