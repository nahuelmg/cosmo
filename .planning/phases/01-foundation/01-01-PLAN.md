---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - .gitignore
  - README.md
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - eslint.config.mjs
  - next-env.d.ts
autonomous: true

must_haves:
  truths:
    - "pnpm install completes without errors"
    - "pnpm typecheck reports zero errors"
    - "pnpm dev starts Next.js on http://localhost:3000"
    - "pnpm build produces a successful build"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest and scripts"
      contains: "\"next\": \"^16"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: "\"strict\": true"
    - path: "next.config.ts"
      provides: "Next.js config (pre-i18n, plain)"
      exports: ["default"]
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS integration"
      contains: "@tailwindcss/postcss"
    - path: "src/app/globals.css"
      provides: "Minimal Tailwind v4 import (replaced in Plan 01-04)"
      contains: "@import \"tailwindcss\""
    - path: ".gitignore"
      provides: "Ignores node_modules, .next, build artefacts, env files"
    - path: "README.md"
      provides: "Project README stub (purpose, dev command, phase status)"
  key_links:
    - from: "package.json"
      to: "next@16.x"
      via: "dependencies"
      pattern: "\"next\"\\s*:\\s*\"\\^16"
    - from: "tsconfig.json"
      to: "strict mode"
      via: "compilerOptions.strict"
      pattern: "\"strict\"\\s*:\\s*true"
    - from: "postcss.config.mjs"
      to: "@tailwindcss/postcss"
      via: "plugins"
      pattern: "@tailwindcss/postcss"
---

<objective>
Scaffold a Next.js 16 + TypeScript strict + Tailwind v4 project at the repo root with pnpm, so every subsequent plan in Phase 1 has a compiling base to build on.

Purpose: Establish FOUND-01 — the project boots, types compile with zero errors, and `pnpm dev` runs. This is the root of the Phase 1 dependency graph; Plans 01-03 and 01-04 both depend on this.

Output: A minimal but complete Next.js 16 project at the repo root with all tooling wired (TypeScript strict, Tailwind v4 via PostCSS, ESLint), a placeholder root `src/app/layout.tsx` and `src/app/page.tsx` (both replaced by Plan 01-04 when `[locale]` shell lands), a `.gitignore`, a README stub, and package scripts (`dev`, `build`, `start`, `lint`, `typecheck`).
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with create-next-app</name>
  <files>
    package.json
    pnpm-lock.yaml
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    eslint.config.mjs
    next-env.d.ts
    .gitignore
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    public/
  </files>
  <action>
Run `create-next-app` non-interactively IN THE REPO ROOT (not a subfolder — `.planning/`, `skills/`, `references/`, `ui-ux-pro-max/` already exist and must be preserved). Use `.` as the project name so it scaffolds in place:

```bash
pnpm create next-app@16 . \
  --typescript \
  --tailwind \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --eslint \
  --use-pnpm \
  --turbopack \
  --yes
```

If `create-next-app` refuses to run because the directory is non-empty, the failure path is: (1) list what it's complaining about, (2) confirm the offending files are only `.planning/`, `skills/`, `references/`, `CLAUDE.md`, `GUIDE.md`, `README.md`, `ui-ux-pro-max/`, `setup.sh`, `extract-learnings.sh`, `.git/` — all of which must survive — and (3) use the `--yes` / overwrite flag or pass `--empty` semantics; if still blocked, fall back to `pnpm dlx create-next-app@16 _tmp-scaffold ...` and move the generated files into the repo root with `rsync -a _tmp-scaffold/ ./ && rm -rf _tmp-scaffold` (do NOT overwrite pre-existing files — `README.md`, `.gitignore`).

After scaffolding, if `create-next-app` produced its own `README.md`, `.gitignore`, or `CLAUDE.md`:
- Merge its `.gitignore` entries into the existing `.gitignore` (dedupe).
- Discard its boilerplate `README.md` (we replace it in Task 2).
- Leave `CLAUDE.md` untouched (it's the project's instructions file).

Post-scaffold sanity:
- Verify `package.json` has `"next": "^16..."`, `"react": "^19..."`, `"typescript": "^5..."`, `"tailwindcss": "^4..."`, `"@tailwindcss/postcss": "^4..."`.
- Verify `tsconfig.json` has `"strict": true`. If it has `strict: false` or omits it, set it to `true`.
- Verify `postcss.config.mjs` declares `@tailwindcss/postcss` (NOT `autoprefixer` — Tailwind v4 handles that).
- Verify `src/app/globals.css` starts with `@import "tailwindcss";` (leave the rest as scaffolded — Plan 01-04 replaces this file with the real OKLCH token set).
- Verify `next.config.ts` exists and is a minimal `NextConfig` export (no i18n plugin yet — Plan 01-03 adds that).
- Delete any Next.js starter boilerplate styling in `src/app/page.tsx` that references example hero images or Vercel logos; leave a trivial `<main><h1>Cosmology Group — scaffolding</h1></main>` placeholder. These files get deleted in Plan 01-04 when `[locale]/` takes over, so don't polish them.
- Delete `src/app/favicon.ico` only if `create-next-app` shipped one (it's fine to keep; not load-bearing for Phase 1).

DO NOT:
- Add next-intl here (Plan 01-03).
- Add `next/font` wiring here (Plan 01-04).
- Replace `globals.css` with OKLCH tokens here (Plan 01-04).
- Create `src/app/[locale]/` here (Plan 01-04).
  </action>
  <verify>
```bash
# Dependencies installed and scripts recognised
pnpm install
pnpm typecheck
pnpm lint

# Dev server boots (start in background, curl, kill)
pnpm dev &
DEV_PID=$!
sleep 8
curl -sI http://localhost:3000 | head -1   # expect 200 OK
kill $DEV_PID

# Build passes
pnpm build
```

All four commands must succeed. `pnpm typecheck` must print zero errors. `pnpm build` must print "Compiled successfully".
  </verify>
  <done>
- `package.json` exists at repo root with `next@^16`, `react@^19`, `typescript@^5`, `tailwindcss@^4`, `@tailwindcss/postcss@^4`.
- `tsconfig.json` has `"strict": true` and `paths: {"@/*": ["./src/*"]}`.
- `next.config.ts` exists and exports a plain `NextConfig`.
- `postcss.config.mjs` declares `@tailwindcss/postcss`.
- `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css` exist as minimal placeholders (replaced by Plan 01-04).
- `pnpm dev`, `pnpm build`, `pnpm typecheck`, `pnpm lint` all succeed.
- Pre-existing files (`.planning/`, `skills/`, `references/`, `CLAUDE.md`, `GUIDE.md`, `ui-ux-pro-max/`, `setup.sh`, `extract-learnings.sh`, `.git/`) are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write project scripts, .gitignore, README stub</name>
  <files>
    package.json
    .gitignore
    README.md
  </files>
  <action>
**`package.json` scripts block** — ensure exactly these scripts are present (merge with what `create-next-app` wrote, keeping its `dev`/`build`/`start`/`lint` if identical):

```json
"scripts": {
  "dev": "next dev --turbopack",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "typecheck": "tsc --noEmit"
}
```

Do NOT add `check-translations` here — Plan 01-03 adds it (it requires `@lingual/i18n-check` which that plan installs).

**`.gitignore`** — ensure the following entries are present (append missing ones; dedupe):

```
# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage/

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local
.env
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/
.idea/

# OS
Thumbs.db
```

Do NOT add `.planning/` — per `.planning/config.json` `commit_docs: true`, planning docs are committed.

**`README.md`** — REPLACE whatever `create-next-app` scaffolded (Vercel boilerplate) with this content. If a `README.md` already existed at repo root (project-level overview), merge rather than overwrite — keep any project description that predates scaffolding and append the Development section below.

```markdown
# Cosmology Group — UBA / FCEN

Bilingual (Spanish default, English toggle) institutional website for the Cosmology Group at the Facultad de Ciencias Exactas y Naturales, Universidad de Buenos Aires.

## Development

```bash
pnpm install
pnpm dev         # http://localhost:3000
pnpm build
pnpm typecheck
pnpm lint
```

## Stack

- Next.js 16 (App Router) + TypeScript strict
- Tailwind v4 (`@theme` CSS-first) + OKLCH design tokens
- next-intl 4.9+ for i18n (Spanish default, English toggle)
- Self-hosted variable fonts via `next/font/google`

## Project Layout

- `src/app/[locale]/` — locale-prefixed pages
- `src/i18n/` — next-intl routing + navigation + request config
- `src/proxy.ts` — next-intl middleware (Next.js 16 renamed `middleware.ts` → `proxy.ts`)
- `messages/{es,en}.json` — UI chrome translations
- `content/` — Zod-validated JSON content (Phase 2+)
- `design-system/` — `ui-ux-pro-max` MASTER.md source of truth
- `.planning/` — GSD workflow docs (roadmap, requirements, phase plans)

## Status

Phase 1 in progress — see `.planning/ROADMAP.md`.
```
  </action>
  <verify>
```bash
# Scripts present
jq '.scripts' package.json | grep -q '"dev":' && \
jq '.scripts' package.json | grep -q '"typecheck":'

# .gitignore covers .next and env files
grep -q "^\.next/" .gitignore && grep -q "\.env" .gitignore

# README exists and mentions the project
grep -q "Cosmology Group" README.md

# Scripts run
pnpm typecheck
pnpm lint
```
  </verify>
  <done>
- `package.json` has `dev`, `build`, `start`, `lint`, `typecheck` scripts.
- `.gitignore` covers Next.js build artefacts, env files, editor caches, node_modules.
- `README.md` describes the project, stack, layout, and development commands.
- `pnpm typecheck` and `pnpm lint` still succeed.
  </done>
</task>

</tasks>

<verification>
Final phase-01-01 checks:

1. `pnpm install` completes cleanly.
2. `pnpm typecheck` exits 0 with no diagnostics.
3. `pnpm lint` exits 0.
4. `pnpm dev` serves http://localhost:3000 with HTTP 200 (placeholder page, English or Spanish irrelevant — i18n arrives in Plan 01-03).
5. `pnpm build` compiles successfully.
6. Pre-existing repo contents (`.planning/`, `skills/`, `references/`, `CLAUDE.md`, `GUIDE.md`, `ui-ux-pro-max/`) are untouched.
</verification>

<success_criteria>
- Repo root contains a working Next.js 16 + TypeScript strict + Tailwind v4 scaffold.
- All npm scripts (`dev`, `build`, `start`, `lint`, `typecheck`) pass.
- `tsconfig.json` enforces `"strict": true`.
- `postcss.config.mjs` uses `@tailwindcss/postcss` (no `autoprefixer`).
- `.gitignore` and `README.md` are project-appropriate.
- No i18n, no fonts, no OKLCH tokens, no `[locale]/` — those belong to 01-03 and 01-04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the template at `/home/tomas/.claude/get-shit-done/templates/summary.md`. Fill the frontmatter with:
- `subsystem: foundation/scaffold`
- `affects: [01-03, 01-04]`
- `tech-stack.added: [next@16, react@19, typescript@5, tailwindcss@4, @tailwindcss/postcss@4, eslint-config-next]`
- Key files: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `src/app/{layout,page,globals.css}.tsx`
</output>
</content>
</invoke>