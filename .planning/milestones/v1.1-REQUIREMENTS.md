# Requirements Archive: v1.1 arXiv + InspireHEP Publication Sync

**Archived:** 2026-04-19
**Status:** ✅ SHIPPED

This is the archived requirements specification for v1.1.
For current requirements, see `.planning/REQUIREMENTS.md` (created for next milestone).

---

**Defined:** 2026-04-18
**Milestone goal:** Auto-populate publications from InspireHEP + arXiv for each current PI, postdoc, and PhD, refreshed weekly at build time, while preserving v1.0's fully-static SSG guarantee.

**Locked decisions:**
- Sync mode: build-time GitHub Action (weekly cron) — no runtime ISR
- Sources: InspireHEP + arXiv, entries source-tagged, no cross-source DOI dedup
- Author linkage: explicit `arxiv_id` + `inspirehep_id` fields in `content/people.json` (arxiv_id reconceived to orcid_id mid-Phase-7 after real-data check)
- Failure mode: fall back to last-good `content/publications.json` (do not commit on failure)
- Profile display: `/people/[slug]` shows last-10-years subset filtered by author match
- Scope: current members only (PIs / postdocs / PhDs)
- Differentiator shipped: author-list truncation respects member-visibility invariant (member-bold softened per user feedback)
- Differentiators deferred: per-person exclude list, "View on InspireHEP/arXiv" profile links, Action failure notifications

---

## v1.1 Requirements

### Schema Extension (atomic prerequisite)

- [x] **SCHEMA-01**: `PublicationSchema` extended with `source: z.enum(["manual", "inspirehep", "arxiv"]).default("manual")` — existing v1.0 entries parse without modification
- [x] **SCHEMA-02**: `PublicationSchema` arxiv ID regex accepts both modern (`2306.12345`) and pre-2007 (`gr-qc/9209007`) formats
- [x] **SCHEMA-03**: `PersonSchema` extended with top-level optional `orcid_id?: string` (reconceived from `arxiv_id` after real-data check — arXiv author-page slugs unavailable) and `inspirehep_id?: string` (BAI format, widened regex accepts multi-segment names)
- [x] **SCHEMA-04**: `PersonSchema.publications_selected` marked `@deprecated` via JSDoc; field still tolerated; sync script logs warning if populated
- [x] **SCHEMA-05**: `content/publications.schema.json` + `content/people.schema.json` regenerated from Zod in the same commit as schema changes
- [x] **SCHEMA-06**: `pnpm validate-content` passes on existing v1.0 JSON content with no data changes

### Content Data (human dependency)

- [~] **DATA-09**: `content/people.json` populated with `inspirehep_id` (BAI format) — PARTIAL: 9 of 13 sync-scoped current members (esteban-calzetta, diana-lopez-nacir, susana-landau, cecilia-scannapieco, nahuel-miron-granese, javier-badia, tomas-ferreira-chase, matias-leizerovich, augusto-chantada). 4 remaining (juan-manuel-armaleo, gonzalo-santa-cruz, guadalupe-ahumada-acuna, juan-pablo-elia) → **v1.2 content task**. Sync scope = pi/postdoc/phd per Phase 9 decision; 2 undergrads excluded intentionally.
- [~] **DATA-10**: `content/people.json` populated with `orcid_id` (reconceived from arxiv_id) — PARTIAL: same 9 members carry orcid_id; same 4 remaining slugs as DATA-09 → **v1.2 content task**.

### Accessor (helper library)

- [x] **ACC-01**: `getPublicationsByAuthor(nameVariants: string[], options?: { lastNYears?: number }): Publication[]` in `src/content/accessors/publications.ts`
- [x] **ACC-02**: Accessor filters publications where any author name matches any `nameVariants` string (case-insensitive, diacritic-normalized via NFD-strip)
- [x] **ACC-03**: `lastNYears` option cuts results to the last N calendar years counted from `new Date().getFullYear()`
- [x] **ACC-04**: Accessor re-exported from `src/content/index.ts` barrel (24-symbol surface)
- [x] **ACC-05**: Accessor has no imports from `people.ts` (avoids circular dependency)

### Sync Script

- [x] **SYNC-01**: `scripts/sync-publications.ts` runnable via `pnpm exec tsx scripts/sync-publications.ts` and `pnpm sync-publications`
- [x] **SYNC-02**: `fast-xml-parser@^5.7.1` added as devDependency with `isArray` callback for arXiv Atom `entry` / `author` / `link` / `category` nodes
- [x] **SYNC-03**: Script fetches InspireHEP literature per `inspirehep_id` with concurrency-limited queue (max 5 parallel), 2s inter-batch pause, exponential backoff on HTTP 429
- [x] **SYNC-04**: Script reads `hits.total` from first InspireHEP response and paginates until complete
- [x] **SYNC-05**: Script fetches arXiv for ORCID-populated members only; skips and logs warning when missing (never falls back to name-based `au:` search)
- [x] **SYNC-06**: Script validates `inspirehep_id` values match the BAI format at startup; exits with clear error on `INSPIRE-00...` numeric IDs
- [x] **SYNC-07**: Script extracts publication year from InspireHEP `publication_info[0].year` with fallback to `preprint_date`; from arXiv `<published>` (version 1) date
- [x] **SYNC-08**: Script normalizes author strings with `.normalize("NFC")`; strips BibTeX markup from titles
- [x] **SYNC-09**: Script performs intra-run dedup by arXiv ID within each source (prevents shared papers appearing once per co-authoring member)
- [x] **SYNC-10**: Script sorts publications deterministically (year desc, then arxiv ID alpha) before serialization
- [x] **SYNC-11**: Script validates the full result with `PublicationsFileSchema.safeParse()` in memory before writing; exits non-zero without writing on failure
- [x] **SYNC-12**: Script writes `content/publications.json` with `_meta: { synced_at, sources[], counts, warnings[] }` (shape improved over spec `{synced_at, status}` — intentional)
- [x] **SYNC-13**: Script tags each entry with the appropriate `source: "inspirehep" | "arxiv"`
- [x] **SYNC-14**: Every request uses `AbortSignal.timeout(10_000)`; script exits non-zero and preserves last-good JSON on any upstream failure
- [x] **SYNC-15**: Script emits summary line "N publications (X added, Y removed, Z unchanged, W warnings)"

### GitHub Action (CI automation)

- [x] **CI-01**: `.github/workflows/sync-publications.yml` scheduled at `0 6 * * 1` (Monday 06:00 UTC) with `workflow_dispatch` for manual runs
- [x] **CI-02**: Workflow declares `permissions: contents: write`
- [x] **CI-03**: Workflow sets up pnpm (`pnpm/action-setup@v4`) and Node 20, installs with `--frozen-lockfile`, runs the sync script (versions pinned to project's `engines.node`; original spec said `v5`/Node 22 before project pinned Node 20)
- [x] **CI-04**: Workflow runs `pnpm validate-content` after sync writes JSON (package.json uses `validate-content`; original spec said `check-content` which never existed)
- [x] **CI-05**: Workflow uses a payload-aware diff check (`jq -cS '.publications'` vs HEAD + `git checkout HEAD -- ...` if unchanged) to skip commit on identical publications data — ignores `_meta.synced_at` byte drift
- [x] **CI-06**: Workflow commits with message including `[skip ci]` (prevents push from triggering a second workflow run)
- [x] **CI-07**: Workflow step summary reports per-run delta ("X added, Y removed") or "No changes"; sync failures leave `content/publications.json` untouched
- [x] **CI-08**: Manual `workflow_dispatch` verified to push successfully

### Publications Page (/publications)

- [x] **PUBS-05**: `/publications` renders the synced archive grouped by year, newest first
- [x] **PUBS-06**: Each entry renders a source badge ("InspireHEP" / "arXiv" / "Manual") — small pill, text-xs, accessible
- [x] **PUBS-07**: Source filter toggle ("Todos / InspireHEP / arXiv / Manual") filters in-browser — no URL state persistence (PUBS-04 still deferred)
- [x] **PUBS-08**: Preprint vs published indicator per entry
- [x] **PUBS-09**: "Actualizado el [date]" staleness indicator rendered from `_meta.synced_at` with `Intl.DateTimeFormat`
- [x] **PUBS-10**: Bilingual footnote explains two-source design
- [x] **PUBS-11**: Author list formatted as: full list if ≤5 authors; "head-through-member, et al." if >5 (member-visibility invariant)
- [~] **PUBS-12**: Member-author bold highlighting SOFTENED per user feedback in Phase 12 polish (bold read as visually confusing against serif body type). Member-visibility-under-`et al.` invariant (PUBS-11) still honored via `formatAuthors` + `buildMemberSurnameSet`. Softening mirrors PEOP-14 pattern (not a gap).

### Person Profile Page (/people/[slug])

- [x] **PEOP-13**: `/people/[slug]` renders a "Publications" section listing the person's last-10-years output; section hidden entirely when list is empty (intentional during DATA-09/10 rollout)
- [~] **PEOP-14**: Count subtitle SOFTENED per 11-CONTEXT decision. Heading is bare `Publicaciones` / `Publications` — the list's own length does the communication. Documented in 11-CONTEXT.md, 11-03-PLAN.md, 11-03-SUMMARY.md, 11-VERIFICATION.md (not a gap).
- [x] **PEOP-15**: Profile entries render same source badge as `/publications` (shared `PublicationEntry.tsx`)
- [x] **PEOP-16**: Profile entries render preprint vs published indicator (shared component)
- [x] **PEOP-17**: `generateStaticParams` category filter (pi / postdoc / phd) preserved — no profile pages for past members
- [x] **PEOP-18**: v1.0 manual "selected publications" list (driven by `publications_selected`) REPLACED by the synced last-10-years list — `getPublicationById` import removed, legacy ~66-line render block stripped

### Bilingual (i18n drift prevention)

- [x] **I18N-08**: All new UI strings present in both locales: source badge labels, preprint/published indicators, filter labels, `updatedAt` ICU format, two-source footnote
- [x] **I18N-09**: `pnpm check-translations` passes with zero key drift

### Documentation

- [x] **DOC-01**: Maintainer note in `content/SYNC.md` explains how to add `inspirehep_id` + `orcid_id` to `people.json`, with paste-ready example entry (L107-130) and BAI/ORCID lookup workflow
- [x] **DOC-02**: Operational Troubleshooting section in `content/SYNC.md` (L138-237) covers: manual `workflow_dispatch` steps, step-summary interpretation, failed-cron diagnosis (BAI format, 429 retry, Atom parse, validate-content gate), local `--dry-run` fallback

---

## Deferred to v1.2+

### Publication Sync Expansion
- **SYNC-DEFER-01**: ORCID-first author lookup (partial in v1.1 — already used for arXiv feed; "ORCID-first" remains conceptually deferred)
- **SYNC-DEFER-02**: NASA ADS API (astrophysics breadth beyond HEP cosmology)
- **SYNC-DEFER-03**: Cross-source DOI dedup (merge InspireHEP + arXiv records of the same paper)
- **SYNC-DEFER-04**: Citation count display
- **SYNC-DEFER-05**: h-index badges (deliberately out — self-promotional tone risk)

### Operational Hardening
- **OPS-DEFER-01**: Action failure notification (email or Slack webhook) — currently relies on GitHub's built-in cron failure notifications
- **OPS-DEFER-02**: Per-person exclusion list (`exclude_arxiv_ids: []` in people.json)

### Profile Enhancements
- **PEOP-DEFER-01**: "View on InspireHEP / arXiv" live-profile links on `/people/[slug]`

### Code Cleanup (v1.2)
- Remove `publications_selected` Zod field from `src/content/schemas/people.schema.ts`
- Remove orphaned accessor exports: `getPublicationById`, `getPublicationsByTopic`, `getAllTopics`
- Remove dead i18n key `people.selectedPublications` from both locale files
- Update REQUIREMENTS.md description to reflect direct-to-main push (not PR flow)

### From v1.0
- **PUBS-03**: Filter controls for year / author / topic on `/publications` (UI filter only; source filter ships in v1.1 under PUBS-07)
- **PUBS-04**: Filter state reflected in URL query params

---

## Out of Scope (v1.1)

| Feature | Reason |
|---------|--------|
| Runtime ISR / on-demand revalidation | Build-time Action preserves SSG (PERF-01); ISR adds Vercel function cost + rate-limit risk |
| PR-based sync (human review step) | Group publishing cadence is slow; auto-commit with Zod validation + Vercel build guard is safe enough |
| Past-member sync | Past members stay a flat list; no profile pages to target |
| Cross-source dedup | Source-tagged separate entries is v1.1's explicit design choice |
| Citation count surfaces | Stale data risk + self-promotional; link to live InspireHEP profile for current counts |
| Full conference proceedings by default | Dilutes peer-reviewed signal; add opt-in flag only if requested |

---

## Traceability (Final Status)

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| SCHEMA-01 | Phase 7 | Complete |
| SCHEMA-02 | Phase 7 | Complete |
| SCHEMA-03 | Phase 7 | Complete (arxiv_id reconceived to orcid_id) |
| SCHEMA-04 | Phase 7 | Complete |
| SCHEMA-05 | Phase 7 | Complete |
| SCHEMA-06 | Phase 7 | Complete |
| DATA-09 | Phase 7 / extended Phase 12 | Partial (9/13 sync-scoped; 4 deferred to v1.2) |
| DATA-10 | Phase 7 / extended Phase 12 | Partial (9/13 sync-scoped; 4 deferred to v1.2) |
| ACC-01…ACC-05 | Phase 8 | Complete |
| SYNC-01…SYNC-15 | Phase 9 | Complete |
| CI-01…CI-08 | Phase 10 | Complete |
| PUBS-05…PUBS-11 | Phase 11 | Complete |
| PUBS-12 | Phase 11 / softened Phase 12 | Softened (member bold dropped per user feedback) |
| PEOP-13 | Phase 11 | Complete |
| PEOP-14 | Phase 11 | Softened (count subtitle dropped per 11-CONTEXT) |
| PEOP-15…PEOP-18 | Phase 11 | Complete |
| I18N-08, I18N-09 | Phase 11 | Complete |
| DOC-01 | Phase 11 / closed Phase 12 | Complete |
| DOC-02 | Phase 11 / closed Phase 12 | Complete |

---

## Milestone Summary

**Shipped:** 45 of 48 v1.1 requirements fully complete; 2 softened (PEOP-14 count subtitle, PUBS-12 member bold) per user feedback and 11-CONTEXT; 2 partial (DATA-09/10 at 9/13 sync-scoped members, 4 remaining deferred to v1.2 as content task).

**Adjusted during milestone:**
- SCHEMA-03: `arxiv_id` reconceived to `orcid_id` in Phase 7-02 after real-data check revealed arXiv author slugs are not reliably discoverable (ORCID-indexed arXiv Atom feed replaces it)
- PEOP-14: count subtitle dropped per 11-CONTEXT (heading is bare "Publicaciones" / "Publications")
- PUBS-12: member-author bold dropped per user feedback in Phase 12 polish (visual readability)

**Dropped:** None.

---
*Archived: 2026-04-19 as part of v1.1 milestone completion*
