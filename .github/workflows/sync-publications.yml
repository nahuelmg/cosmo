name: Sync Publications

on:
  schedule:
    - cron: '0 0 * * *'   # daily 00:00 UTC = 21:00 (9 pm) ART (GMT-3)
  workflow_dispatch:

concurrency:
  group: sync-publications
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '10'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # CI-01: no --no-* flags passed → InspireHEP + arXiv + ORCID all run by default.
      # To skip a source in a workflow_dispatch run, add --no-inspire / --no-arxiv / --no-orcid.
      # ORCID is a Phase 16 stub returning []; Phase 17 wires the real ORCID fetch.
      - name: Run sync script
        run: |
          pnpm sync-publications > /tmp/sync-stdout.txt 2>/tmp/sync-stderr.txt
          cat /tmp/sync-stdout.txt
          cat /tmp/sync-stderr.txt >&2

      - name: Validate content
        run: pnpm validate-content

      - name: Check for changes (publications payload only; ignore synced_at drift)
        id: diff
        run: |
          NEW_PUBS=$(jq -cS '.publications' content/publications.json)
          OLD_PUBS=$(git show HEAD:content/publications.json 2>/dev/null | jq -cS '.publications' 2>/dev/null || echo "null")
          if [ "$NEW_PUBS" = "$OLD_PUBS" ]; then
            git checkout HEAD -- content/publications.json
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Configure git identity
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Commit changes
        if: steps.diff.outputs.changed == 'true'
        run: |
          git add content/publications.json
          git commit -m "chore(publications): sync daily feed [skip ci]"

      - name: Push to main
        if: steps.diff.outputs.changed == 'true'
        run: git push origin main

      - name: Write step summary
        if: always()
        run: |
          if [ "${{ steps.diff.outputs.changed }}" = "true" ]; then
            SYNC_LINE=$(tail -1 /tmp/sync-stdout.txt)
            SYNCED_AT=$(jq -r '._meta.synced_at' content/publications.json 2>/dev/null || echo "unknown")
            echo "${SYNC_LINE}" >> $GITHUB_STEP_SUMMARY
            WARNINGS=$(grep '^Warning:' /tmp/sync-stderr.txt 2>/dev/null || true)
            if [ -n "$WARNINGS" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "## Warnings" >> $GITHUB_STEP_SUMMARY
              while IFS= read -r line; do
                echo "- ${line#Warning: }" >> $GITHUB_STEP_SUMMARY
              done <<< "$WARNINGS"
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Synced at ${SYNCED_AT}" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ steps.diff.outputs.changed }}" = "false" ]; then
            echo "No changes — skipping commit" >> $GITHUB_STEP_SUMMARY
            SYNCED_AT=$(jq -r '._meta.synced_at' content/publications.json 2>/dev/null || echo "unknown")
            echo "Synced at ${SYNCED_AT}" >> $GITHUB_STEP_SUMMARY
          else
            echo "Sync failed — see logs" >> $GITHUB_STEP_SUMMARY
          fi
